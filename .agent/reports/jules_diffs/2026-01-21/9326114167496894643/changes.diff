diff --git a/praxis/web-client/src/app/features/run-protocol/components/run-protocol.component.ts b/praxis/web-client/src/app/features/run-protocol/components/run-protocol.component.ts
index 07c7580..736ba03 100644
--- a/praxis/web-client/src/app/features/run-protocol/components/run-protocol.component.ts
+++ b/praxis/web-client/src/app/features/run-protocol/components/run-protocol.component.ts
@@ -15,6 +15,7 @@ import { ProtocolService } from '../../protocols/services/protocol.service';
 import { ProtocolDefinition } from '../../protocols/models/protocol.models';
 import { ExecutionService } from '../services/execution.service';
 import { Observable } from 'rxjs';
+import { ErrorBoundaryComponent } from '../../../shared/components/error-boundary/error-boundary.component';
 
 @Component({
   selector: 'app-run-protocol',
@@ -29,17 +30,20 @@ import { Observable } from 'rxjs';
     MatButtonModule,
     MatIconModule,
     MatCardModule,
-    MatProgressSpinnerModule
+    MatProgressSpinnerModule,
+    ErrorBoundaryComponent
   ],
   template: `
     <div class="p-6">
       <h1 class="text-3xl font-bold mb-6">Run Protocol</h1>
 
-      <mat-stepper linear #stepper class="glass-panel">
-        <!-- Step 1: Select Protocol -->
-        <mat-step [stepControl]="protocolForm">
-          <ng-template matStepLabel>Select Protocol</ng-template>
-          <form [formGroup]="protocolForm" class="py-6">
+      <app-error-boundary [error]="executionError()" (retry)="clearError()" (dismiss)="clearError()">
+        <ng-template>
+          <mat-stepper linear #stepper class="glass-panel">
+            <!-- Step 1: Select Protocol -->
+            <mat-step [stepControl]="protocolForm">
+              <ng-template matStepLabel>Select Protocol</ng-template>
+              <form [formGroup]="protocolForm" class="py-6">
             <mat-form-field appearance="outline" class="w-full max-w-md">
               <mat-label>Protocol</mat-label>
               <mat-select formControlName="protocol" (selectionChange)="onProtocolSelect($event.value)">
@@ -126,6 +130,8 @@ import { Observable } from 'rxjs';
           </div>
         </mat-step>
       </mat-stepper>
+    </ng-template>
+  </app-error-boundary>
     </div>
   `
 })
@@ -138,6 +144,7 @@ export class RunProtocolComponent implements OnInit {
   protocols$!: Observable<ProtocolDefinition[]>;
   selectedProtocol = signal<ProtocolDefinition | null>(null);
   isExecuting = signal(false);
+  executionError = signal<Error | null>(null);
 
   protocolForm: FormGroup = this.fb.group({
     protocol: [null, Validators.required]
@@ -169,8 +176,13 @@ export class RunProtocolComponent implements OnInit {
       },
       error: (err) => {
         this.isExecuting.set(false);
+        this.executionError.set(err);
         console.error('Failed to start run:', err);
       }
     });
   }
+
+  clearError() {
+    this.executionError.set(null);
+  }
 }
diff --git a/praxis/web-client/src/app/shared/components/error-boundary/error-boundary.component.html b/praxis/web-client/src/app/shared/components/error-boundary/error-boundary.component.html
new file mode 100644
index 0000000..ffdd56e
--- /dev/null
+++ b/praxis/web-client/src/app/shared/components/error-boundary/error-boundary.component.html
@@ -0,0 +1,18 @@
+<ng-container *ngIf="!hasError; else errorTemplate">
+  <ng-container *ngTemplateOutlet="content"></ng-container>
+</ng-container>
+
+<ng-template #errorTemplate>
+  <mat-card class="error-container">
+    <mat-card-header>
+      <mat-card-title>Something went wrong</mat-card-title>
+    </mat-card-header>
+    <mat-card-content>
+      <p>An unexpected error has occurred. Please try again.</p>
+    </mat-card-content>
+    <mat-card-actions class="error-actions">
+      <button mat-raised-button color="primary" (click)="onRetry()">Retry</button>
+      <button mat-button (click)="onDismiss()">Dismiss</button>
+    </mat-card-actions>
+  </mat-card>
+</ng-template>
diff --git a/praxis/web-client/src/app/shared/components/error-boundary/error-boundary.component.scss b/praxis/web-client/src/app/shared/components/error-boundary/error-boundary.component.scss
new file mode 100644
index 0000000..c5a5c85
--- /dev/null
+++ b/praxis/web-client/src/app/shared/components/error-boundary/error-boundary.component.scss
@@ -0,0 +1,25 @@
+:host {
+  display: flex;
+  justify-content: center;
+  align-items: center;
+  width: 100%;
+  height: 100%;
+}
+
+.error-container {
+  max-width: 500px;
+  text-align: center;
+  background: var(--glass-bg);
+  border: var(--glass-border);
+  box-shadow: var(--glass-shadow);
+  backdrop-filter: var(--glass-blur);
+  -webkit-backdrop-filter: var(--glass-blur);
+  border-radius: 10px;
+}
+
+.error-actions {
+  display: flex;
+  justify-content: flex-end;
+  gap: 8px;
+  padding: 8px 0;
+}
diff --git a/praxis/web-client/src/app/shared/components/error-boundary/error-boundary.component.ts b/praxis/web-client/src/app/shared/components/error-boundary/error-boundary.component.ts
new file mode 100644
index 0000000..d94ec61
--- /dev/null
+++ b/praxis/web-client/src/app/shared/components/error-boundary/error-boundary.component.ts
@@ -0,0 +1,42 @@
+import { Component, ChangeDetectionStrategy, Input, ContentChild, TemplateRef, ChangeDetectorRef, Output, EventEmitter } from '@angular/core';
+import { CommonModule } from '@angular/common';
+import { MatCardModule } from '@angular/material/card';
+import { MatButtonModule } from '@angular/material/button';
+
+@Component({
+  selector: 'app-error-boundary',
+  templateUrl: './error-boundary.component.html',
+  styleUrls: ['./error-boundary.component.scss'],
+  changeDetection: ChangeDetectionStrategy.OnPush,
+  standalone: true,
+  imports: [CommonModule, MatCardModule, MatButtonModule],
+})
+export class ErrorBoundaryComponent {
+  @Input()
+  set error(e: Error | null) {
+    this.hasError = !!e;
+    if (this.hasError) {
+      console.error(e);
+      this.cdr.detectChanges();
+    }
+  }
+
+  @ContentChild(TemplateRef) content!: TemplateRef<unknown>;
+
+  @Output() retry = new EventEmitter<void>();
+  @Output() dismiss = new EventEmitter<void>();
+
+  hasError = false;
+
+  constructor(private readonly cdr: ChangeDetectorRef) {}
+
+  onRetry(): void {
+    this.hasError = false;
+    this.retry.emit();
+  }
+
+  onDismiss(): void {
+    this.hasError = false;
+    this.dismiss.emit();
+  }
+}

