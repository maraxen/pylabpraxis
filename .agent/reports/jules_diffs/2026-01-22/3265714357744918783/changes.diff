diff --git a/tests/core/test_orchestrator.py b/tests/core/test_orchestrator.py
index c6c90c6..ad79d0b 100644
--- a/tests/core/test_orchestrator.py
+++ b/tests/core/test_orchestrator.py
@@ -188,6 +188,7 @@ class TestInjectPraxisState:
 
         # Use mock instead of real PraxisState to avoid Redis
         praxis_state = Mock(spec=PraxisState)
+        praxis_state.set = AsyncMock()
         final_args = {}
 
         orchestrator._inject_praxis_state(
@@ -216,6 +217,7 @@ class TestInjectPraxisState:
 
         # Use mock instead of real PraxisState to avoid Redis
         praxis_state = Mock(spec=PraxisState)
+        praxis_state.set = AsyncMock()
         praxis_state.to_dict.return_value = {"key": "value"}
         final_args = {}
 
@@ -243,6 +245,7 @@ class TestInjectPraxisState:
 
         # Use mock instead of real PraxisState to avoid Redis
         praxis_state = Mock(spec=PraxisState)
+        praxis_state.set = AsyncMock()
         final_args = {}
 
         result = orchestrator._inject_praxis_state(
@@ -724,6 +727,7 @@ class TestHandleProtocolExecutionError:
 
         # Mock PraxisState
         praxis_state = Mock()
+        praxis_state.set = AsyncMock()
         praxis_state.get.return_value = {"snapshot": "data"}
 
         mock_db_session = AsyncMock()
