# Stack Configuration for praxis
# Auto-generated by Orbital Velocity Pipeline 407
# Documents detected technology stacks and standard Justfile recipes

stacks:
  - name: python
    detected: true
    framework: fastapi
  - name: frontend
    detected: true
    framework: vitest
    runtime: bun

standard_recipes:
  test: "bunx vitest"
  test-changed: "bunx vitest --changed"
  lint: "bun run lint"
  build: "bun run build"

# Additional custom recipes available:
# - generate-db: Generate the browser-mode SQLite database using pre-discovered backends and protocols
