---
session_id: edde4648
topic: Visualizer transport shim and augmentation API for the praxis JupyterLite/PLR REPL refocus: design the Python-side transport seam, the augmentation API shape, CDN vendoring, the first concrete augmentation, and upstream-drift detection.
task_type: architectural
winner: A' — "BrowserVisualizer: thin subclass over a BroadcastChannel courier, into a vendored same-origin visualizer iframe, with a three-layer praxisViz augmentation API."

PYTHON SEAM: praxis/viz/browser.py defines BrowserVisualizer(pylabrobot.visualizer.Visualizer) overriding exactly four public/semi-public methods — setup(), stop(), has_connection(), send_command() — plus injecting a _PyodideLoopShim into self._loop. It never names a serializer, so the payload-schema split across pins is invisible to it. All serialization (_serialize_with_methods / _serialize_resource_tree + _build_method_registry), envelope assembly (_assemble_command: id/version/data/event), float sanitisation (_sanitize_floats), did_assign/did_unassign/state_update callback registration, and the 96-channel state-update BATCHING (_enqueue_state_update/_flush_state_updates) are inherited verbatim.

TRANSPORT: send_command() hands the assembled JSON string to an injected VisualizerTransport (Protocol, grafted from C) — BroadcastChannelTransport('praxis_viz') for the browser, plus RecordingTransport / FileTransport / NullTransport for tests. Every transport wraps a bounded ring buffer (grafted from H). The visualizer page announces readiness on the channel and the Python side answers by calling upstream's own _send_resources_and_state(), reusing the "ready" handshake rather than inventing one, which fixes BroadcastChannel's no-replay problem.

DELIVERY: a generated, committed praxis/web-client/src/assets/visualizer/ tree derived from the submodule at PLR 0.2.2 — stock lib.js/main.css/gif.js/gif.worker.js/img, a 4-anchor-patched vis.js, a de-templated index.html, and vendored konva.min.js 8.4.3. Rendered in a sibling same-origin iframe next to the JupyterLite iframe in the new static host page. lib.js is byte-identical to upstream.

AUGMENTATION API: three layers, all spike-verified, none touching lib.js internals — praxisViz.use(fn) middleware wrapping window.handleEvent/processCentralEvent (A1); praxisViz.overlay (a named Konva.Layer) plus praxisViz.anchor(resourceName) implementing the verified inverse-transform (A2); and praxis-namespaced side-channel events emitted from Python alongside the 4 stock ones (A3).

FIRST AUGMENTATION: F3 device-reality overlay. VENDORING: V3 generate-and-commit with a CI git-diff gate. DRIFT: D2 + D4 blocking per-PR, D3 nightly, D1 rejected.
created_at: 2026-08-17T15:29:54.573380+00:00
---

# Brainstorm: Visualizer transport shim and augmentation API for the praxis JupyterLite/PLR REPL refocus: design the Python-side transport seam, the augmentation API shape, CDN vendoring, the first concrete augmentation, and upstream-drift detection.

## Problem Frame
FIXED CONSTRAINTS (cannot change):
- Static site only. No Python HTTP server, no websocket server, no backend. GH Pages is a target, so no COOP/COEP guarantee and no custom headers.
- The renderer runs in the browser main thread (a DOM document); the Python kernel runs in a DedicatedWorkerGlobalScope. `js.window` does NOT exist in the kernel — this is already a confirmed live bug in web_serial_shim.py. So ANY design that assumes the kernel can touch the visualizer DOM directly is dead on arrival. There must be a worker->document boundary crossing.
- lib.js is consumed unmodified (5853 lines). Non-negotiable.
- PLR is a git submodule; the visualizer assets must be derived from it, not hand-copied.
- Renderer and serializer must come from the SAME PLR pin (payload schema differs across pins).
- Konva 8.4.3 must be vendored; unpkg is not reachable in CI-like sandboxes.

NEW EVIDENCE I just gathered by reading external/pylabrobot/pylabrobot/visualizer/visualizer.py at the pinned d9651e2 — this materially changes option (1):
- `send_command(event, data, wait_for_response=False)` is the SINGLE funnel. All four events plus `show_machine_tools` and `stop` go through it (lines 604, 623, 626, 649, 658, 681, 585).
- Everything upstream of that funnel is reusable and NOT socket-aware: `_serialize_with_methods` (or `_serialize_resource_tree`+`_build_method_registry` at 0.2.2), `_sanitize_floats`, `_assemble_command` (adds id/version/data/event), the did_assign/did_unassign/state_update callback registration in `__init__` (lines 140-151), and the state-update BATCHING machinery `_enqueue_state_update`/`_flush_state_updates` (668-682) which exists precisely so a 96-channel pickup is one message.
- The ONLY socket-coupled bits are: `setup()` -> `_run_ws_server()`+`_run_file_server()`; `has_connection()`/`websocket` property; `stop()`'s httpd shutdown; and — this is the trap — three callbacks reach `self.loop` (`asyncio.run_coroutine_threadsafe(fut, self.loop)` at 650 and 659, `self.loop.call_soon_threadsafe` at 666). `self._loop` is set ONLY inside `_run_ws_server`, so a subclass that overrides only send_command still explodes on the first resource assignment.
- The fix is small and I want it on the table: in Pyodide an asyncio event loop IS already running, so a subclass can set `self._loop = asyncio.get_event_loop()` and `self.setup_finished = True` in its own setup(), and then `run_coroutine_threadsafe`/`call_soon_threadsafe` degrade to single-thread `call_soon` and just work. That means the override set is: `setup`, `stop`, `has_connection`, `send_command` — four methods, none of which mention a serializer, so the 485KB-vs-164KB payload-schema divergence across pins is INVISIBLE to us.
- Also note `_detect_source_filename()` runs in `__init__` and tries ipynbname/jupyter_server/urllib against a live Jupyter REST API — in Pyodide we must pass `name=` explicitly to short-circuit it.

NEGOTIABLE:
- Whether we subclass at all vs. reimplement the ~40 lines of callback wiring.
- Where the renderer lives: same-document sibling element, nested iframe, separate browser tab/window, or a notebook output cell (anywidget).
- Whether the channel is one-way (Python -> canvas) or bidirectional.
- Whether augmentations are authored in Python, in JS, or both.
- Which PLR pin we target (d9651e2 / 0.2.2 / main) — 0.2.2 currently looks strongly favoured by the 3x payload shrink and the pylibftdi stub going away.
- How much of the visualizer's own UI chrome (machine tools, sidebar, export) we keep.

## Idea Pool
- [ai] A. "Thin subclass + postMessage courier" (BrowserVisualizer). Python: `praxis_viz.BrowserVisualizer(Visualizer)` overriding exactly setup/stop/has_connection/send_command. setup() sets self._loop = asyncio.get_event_loop(), setup_finished = True, and posts a `viz:hello`. send_command() calls _assemble_command() (reusing upstream id/version/sanitize) then ships the already-serialized JSON string over the worker's postMessage / a BroadcastChannel to the document. Browser: a praxis-owned courier script in the visualizer page turns each message into window.receiveFromPython(event, data). All serialization, callback registration and state batching is inherited verbatim. Renderer lives in a nested iframe (or a JupyterLab sibling panel) holding the vendored PLR visualizer index.html.
- [ai] B. "anywidget/ipywidget embedded canvas". The visualizer is a notebook OUTPUT CELL, not a separate page. A praxis anywidget (`praxis.viz.DeckView`) carries an ESM front-end that injects the vendored lib.js/vis.js/konva into a shadow root or a srcdoc iframe inside the output area. Python->JS transport is the Jupyter comm channel (traitlets `_events` list or `.send()` custom messages), which JupyterLite implements natively over the worker boundary — no bespoke postMessage protocol at all. Augmentations are traitlets on the widget model, so they are bidirectional by construction and survive notebook save/reload as cell metadata.
- [ai] C. "Transport-protocol reimplementation (no subclass)". praxis owns `praxis/viz/attach.py` which does NOT import or subclass Visualizer. It re-implements the ~40 lines of callback wiring (register_did_assign / did_unassign / state_update, plus its own batching) and calls PLR's module-level serialization helpers by name. A `VisualizerTransport` Protocol (send(event, data)) is the injection point, with implementations for postMessage, comm, a no-op recorder, and a file dumper. Zero coupling to Visualizer's class shape; coupling is instead to the module-level helper NAMES and to Resource's callback API.
- [ai] D. "Monkeypatch upstream in place". Leave user code writing stock `Visualizer(deck)`; praxis_bootstrap patches `pylabrobot.visualizer.visualizer.Visualizer.setup/send_command/stop` at import time so ANY notebook — including copy-pasted upstream PLR docs examples — transparently works in the browser. Maximum compatibility with the PLR ecosystem's example corpus; the notebook literally looks like upstream docs.
- [ai] E. "In-browser websocket polyfill". Ship a JS shim that makes `websockets.asyncio.server.serve` work inside Pyodide by faking a loopback socket pair between the worker and the document, so upstream Visualizer runs COMPLETELY unmodified — setup(), _run_ws_server() and all. Zero Python patch surface; the entire drift exposure moves into a fake-socket library.
- [ai] F. "Fork/vendor a praxis renderer". Copy lib.js into praxis and edit it freely; augmentations become first-class renderer features rather than bolt-ons. Unbounded augmentation power, unbounded maintenance, and it violates the stated success criterion that lib.js stay unmodified — named for completeness so the winner is chosen against it, not by default.
- [ai] AUGMENTATION SHAPE A1 — JS event middleware at the message boundary. praxis owns a `praxisViz.use(fn)` chain wrapping window.handleEvent (vis.js:147) / window.processCentralEvent (vis.js:83). Each middleware sees {id, event, data} and may mutate, drop, or observe before the stock renderer runs. Spike-verified wrappable. Depends only on the 4-event public protocol, which is a documented upstream contract, not on lib.js internals.
- [ai] AUGMENTATION SHAPE A2 — Konva overlay layer + resource anchor API. `praxisViz.overlay` is a dedicated Konva.Layer added via stage.add(); praxis exposes `praxisViz.anchor(resourceName) -> {x,y}` implementing the verified inverse-transform (stage.getAbsoluteTransform().copy().invert().point(resources[name].group.getAbsolutePosition())) to undo the Y-flip at lib.js:3218-3219. Augmentations draw freely without touching the stock layers. Depends on 3 globals (stage, resources, and Konva itself) plus each resource's .group.
- [ai] AUGMENTATION SHAPE A3 — Python-side payload enricher. A decorator/hook chain in Python that mutates or extends the serialized tree/state before it leaves send_command, e.g. adding a `praxis` namespace key to each resource dict. Costs nothing in JS but is invisible to the stock renderer (which ignores unknown keys), so it is only useful in combination with a JS consumer. Its real value is a praxis-owned SIDE CHANNEL: emit custom event names ('praxis:trace', 'praxis:annotate') alongside the 4 stock ones.
- [ai] AUGMENTATION SHAPE A4 — Bidirectional command channel (canvas -> Python). The reverse direction: canvas clicks / machine-tool buttons post back to the kernel, which can execute Python. Upstream already ships a `method_registry` of public method signatures per class (0.2.2+) that the visualizer UI presents, implying upstream intends this direction eventually. praxis could expose `praxisViz.onSelect(cb)` in Python and a click-to-select-resource flow. Highest value, highest risk: it needs the kernel to accept unsolicited work while idle (no run loop is executing), and it re-opens the user-gesture question from commit 97a75988.
- [user] Six whole-stack candidates (A-F) and four orthogonal augmentation shapes (A1-A4) are recorded. I also have three sub-axes still to diverge on: CDN/vendoring ownership, the first concrete augmentation, and drift detection. Probe me on the stack candidates first — particularly on where each one's upstream-drift exposure actually lands, because that is the criterion I expect to decide it.
- [ai] VENDORING V1 — "commit the bytes". Check the ~6 visualizer files + konva.min.js into praxis/web-client/src/assets/visualizer/ as tracked files, with a provenance header (source pin SHA + sha256). Simple, works offline, works in a clean clone, no build step. Cost: 160KB+ of vendored JS in git, and a human must remember to re-run the copy after a submodule bump.
- [ai] VENDORING V2 — "derive at build time". A `just viz-vendor` / scripts/vendor_visualizer.py step copies from external/pylabrobot/pylabrobot/visualizer/, applies the vis.js patch, rewrites the 5 CDN tags, substitutes the 4 {{ }} placeholders, copies img/logo.png -> favicon.png, and pulls konva from node_modules (npm dep, not CDN). Output is gitignored; CI runs it. Single source of truth = the submodule pin. Cost: a clean clone cannot serve the site without running the step — which is exactly the failure mode already burning us in assets/jupyterlite/build/.
- [ai] VENDORING V3 — "derive at build time AND commit the output, with a CI drift gate". Same generator as V2, but its output IS committed, and CI re-runs the generator and fails on `git diff --exit-code`. Gets V1's clean-clone-serves property and V2's single-source-of-truth property; the cost is review noise on generated files, mitigated by keeping the generated tree small (6 files) and marking it linguist-generated.
- [ai] VENDORING V4 — "upstream the fix". Send a PR to PyLabRobot adding a browser/embedded transport hook to Visualizer (e.g. an overridable `_transport` or an official `receiveFromPython` entrypoint documented as public), so praxis's patch surface goes to zero over time. Slow, out of our control, but it is the only option that makes drift structurally impossible rather than merely detected.
- [ai] FIRST AUGMENTATION F1 — "time-travel scrubber". web_bridge.py already captures state_before/state_after per function call but only feeds a debug panel. Record the set_state deltas into a ring buffer and expose a slider that replays them onto the canvas, highlighting changed resources. Uses only the stock 4-event protocol replayed backwards; needs no new upstream surface.
- [ai] FIRST AUGMENTATION F2 — "live command highlight / call provenance". Every PLR call the kernel makes (pick_up_tips, aspirate, dispense) emits a praxis side-channel event naming the resources it touched and the source cell; the overlay draws a transient ring on those resources and a caption of the call. Answers "what is my code doing right now on my real robot" — which is the actual pitch of a browser REPL driving hardware. Requires an interception point on the LiquidHandler/backend call path, not just on visualizer state.
- [ai] FIRST AUGMENTATION F3 — "device-reality overlay". Colour each resource/machine by the health of its actual browser transport: WebSerial/WebUSB/WebHID connected, permission not yet granted (needs the 97a75988 user-gesture dialog), or shim inert. This is the ONE augmentation nothing upstream will ever have, because upstream has no browser device layer. It also surfaces the currently-silent WebSerial failure visually instead of as a RuntimeError deep in a traceback.
- [ai] FIRST AUGMENTATION F4 — "click-to-code". Click a well/tip-spot on the canvas and praxis inserts the corresponding Python accessor (plate_01['A1']) into the active notebook cell. Highest teaching value for a REPL product, but it needs the bidirectional channel (A4) and JupyterLab cell-insertion APIs, so it is the most coupled to things the spikes did not test.
- [ai] DRIFT DETECTION D1 — "byte-level tripwire". Store sha256 of each upstream file we depend on (lib.js, vis.js, index.html, main.css, visualizer.py) in a manifest; CI fails when the submodule pin moves and a hash changes. Zero browser needed. Catches everything, including cosmetic changes -> high false-positive rate (lib.js changed +39/-25 between 0.2.2 and main with zero behavioural impact on us).
- [ai] DRIFT DETECTION D2 — "assert-once patch application". The vendoring generator applies each vis.js edit as an anchored textual substitution that ASSERTS it matched exactly once (this is what the spike harness already did successfully across three pins). A pin bump that moves or renames the socket glue fails the build loudly at generate time, with a message naming the anchor that no longer matches. Zero browser, zero false positives, and it fails at exactly the moment a human is already looking.
- [ai] DRIFT DETECTION D3 — "golden-render contract test". A headless-browser test loads the vendored bundle, injects a committed set_root_resource + set_state payload fixture, and asserts invariants: Konva shape count, len(resources) registry, non-white pixel count on the resource layer, and specific fills (tipspot A1 '#40CDA1' -> 'white'). Exactly what the spike measured (396/220/18). Catches semantic breakage that hashes and anchors miss. Cost: needs a browser in CI, and the spike proved Playwright's chromium download is not reachable from a sandboxed environment — so it must pin to a system Chrome / Chrome-for-Testing path, or run as a nightly rather than a per-PR gate.
- [ai] DRIFT DETECTION D4 — "Python-side payload schema snapshot". A browserless pytest that builds a small fixed deck, instantiates the praxis transport with a recording transport, and snapshot-asserts the emitted event names + top-level payload keys (e.g. presence/absence of `method_registry`, `resource`, `state`). This is the test that would have caught the 485KB->164KB schema split BEFORE it silently mismatched a renderer from a different pin. Runs in the existing pytest suite with a -k selector; no browser, no network.
- [user] 23 ideas recorded across four axes: stack (A-F), augmentation shape (A1-A4), vendoring (V1-V4), first augmentation (F1-F4), drift detection (D1-D4).
- [user] I want to force one more orthogonal move before converging, because everything above assumes the visualizer is a PASSIVE MIRROR of a deck object. The orthogonal framings I have not explored:
- [user] G. "Visualizer as the notebook's rich repr". Instead of an explicit Visualizer object, give Deck/Resource a `_repr_mimebundle_` so `deck` on the last line of a cell just renders. The recon confirmed PLR has NO _repr_html_ anywhere, so this is net-new praxis surface with zero upstream conflict — and it makes the visualizer the DEFAULT experience rather than an opt-in three-liner.
- [user] H. "Headless-first: the transport has no browser at all by default". Make the primary transport a RECORDER that accumulates the event stream, and make rendering one of several sinks (canvas, JSON file, replayable .plrviz artifact, pytest assertion). This inverts the design: the browser canvas becomes a consumer of a praxis-owned event log rather than the point of the system. It makes drift testing free (D3/D4 fall out for nothing) and makes the time-travel augmentation (F1) structural rather than bolted on.
- [user] Probe these two, then take me to convergence.
- [ai] G. "Visualizer as rich repr". Give Deck/Resource a `_repr_mimebundle_` (or register an IPython formatter in praxis_bootstrap) so evaluating `deck` in a cell renders the canvas inline with no explicit Visualizer object. Recon confirmed PLR ships NO _repr_html_/_repr_mimebundle_ anywhere, so this is net-new praxis surface with zero upstream conflict. Makes the visualizer the default experience. Risk: a static repr is a snapshot, not a live view, unless it is backed by a widget/comm — so it composes with B, not with A.
- [ai] H. "Headless-first / event-log-centric". Invert the design: the primary artifact is a praxis-owned, append-only event log (the 4 stock events + praxis side-channel events), and rendering is ONE sink among several (canvas, JSON dump, replayable .plrviz file, pytest assertion, future server-side render). The browser canvas consumes the log rather than being the system. Makes D3/D4 drift tests fall out for free, makes F1 time-travel structural rather than bolted-on, and decouples "did we produce the right events" from "did the renderer draw them" — the two failure modes that currently blur together. Cost: an extra indirection nobody asked for if the log never grows a second consumer.
- [user] converge

## Decision Log
- [REJECT] E — In-browser websocket polyfill (run upstream Visualizer completely unmodified): Superficially the lowest patch surface, but it relocates ALL drift exposure into a fake-socket library that must emulate websockets.asyncio.server.serve, an async context manager, ServerConnection.recv/send, and asyncio.exceptions semantics inside Pyodide's partial WebLoop — plus _run_ws_server spawns a threading.Thread and busy-waits on a threading.Lock (visualizer.py:468-475), which cannot work in a worker with no real threads. It also requires the `websockets` package to import successfully in Pyodide (HAS_WEBSOCKETS is currently False there). We would own a harder, less testable surface than the 4 methods we are trying to avoid overriding.
- [REJECT] F — Fork/vendor a praxis renderer (edit lib.js freely): Violates the stated success criterion directly. lib.js is 5853 lines and upstream is ACTIVELY developing it (32 commits in the recon delta; +39/-25 between 0.2.2 and main). A fork means every upstream visualizer improvement becomes a manual merge forever, in exchange for augmentation power we do not need — the spike proved a Konva overlay layer plus event-boundary wrapping already gives us drawing and interception without a single edited line.
- [MERGE] C — Transport-protocol reimplementation, no subclass: Rejected as the primary mechanism: it does not actually reduce drift exposure, it MOVES it from a public class shape to private module-level function names (_serialize_with_methods vs _serialize_resource_tree + _build_method_registry), which the spike proved is the surface that ALREADY changed between pins — exactly the wrong thing to depend on. Subclassing and never naming a serializer is strictly more drift-robust. But C's best idea is grafted wholesale: the `VisualizerTransport` Protocol with swappable implementations (broadcast-channel, recording, file-dump, null) becomes the injected collaborator of the subclass, which is what makes browserless testing (D4) possible at all.
- [DEFER] D — Monkeypatch pylabrobot.visualizer.Visualizer in place at bootstrap: Real value: copy-pasted upstream PLR docs examples (`vis = Visualizer(deck); await vis.setup()`) would just work, which matters a lot for a teaching REPL. But as the PRIMARY mechanism it is worse than a subclass: it makes the transport unmockable in pytest, gives a confusing traceback when it misfires, and means there is no way to opt out. Deferred as a thin, optional, one-line-at-bootstrap ALIAS layer on top of the subclass (`pylabrobot.visualizer.visualizer.Visualizer = BrowserVisualizer`) once BrowserVisualizer is proven — cheap then, dangerous now.
- [ACCEPT] Augmentation shape: adopt A1 + A2 + A3 as one three-layer API; defer A4: A1 (JS event middleware), A2 (Konva overlay + anchor helper) and A3 (Python side-channel events) are not competitors — they are the three segments of one pipeline (produce in Python -> intercept in JS -> draw on canvas), and all three were spike-verified. Critically, all three depend ONLY on things that did not change across three PLR pins: the 4-event protocol, the `stage`/`resources` script-scope globals, and Konva's own public API. None touches lib.js internals. A4 (bidirectional canvas->Python) is DEFERRED: it needs the kernel to accept unsolicited work while idle, re-opens the user-gesture constraint from 97a75988, and nothing in either spike tested it.
- [ACCEPT] Vendoring: V3 (generate AND commit, with CI `git diff --exit-code` gate); V4 pursued in parallel as a slow track: V2's gitignored output reproduces the EXACT failure already live in this repo: assets/jupyterlite/build/ contains only service-worker.js, so a clean clone serves a dead page. That is a demonstrated, not hypothetical, cost. V1 has no single source of truth and silently rots on a submodule bump. V3 gets both properties for the price of six generated files in review. V4 (upstream a transport hook to PyLabRobot) is the only structural fix and is cheap to start, but cannot be depended on for this sprint.
- [ACCEPT] Drift detection: D2 + D4 as blocking per-PR gates; D3 as nightly/opt-in; D1 rejected: D2 (assert-each-vis.js-anchor-matched-exactly-once at generate time) and D4 (browserless payload schema snapshot) are both free of a browser and free of network, catch the two failure modes that actually bite (socket glue moved; payload schema changed), and fail at generate/test time when a human is already looking. D3 (golden render) catches semantic breakage the other two miss but needs a browser, and the spike PROVED Playwright's chromium download is unreachable from a sandboxed environment — so it must be nightly with a pinned system Chrome path, never a per-PR blocker. D1 (sha256 of every upstream file) is rejected as pure noise: lib.js changed +39/-25 between 0.2.2 and main with byte-identical render results (396 shapes/220 resources/18 tree nodes), so it would have fired a false alarm on the single most important real-world bump.
- [ACCEPT] First augmentation: F3 (device-reality overlay), not F1 (time-travel scrubber): F1's data source (web_bridge state_before/state_after) sits on the manifest-driven protocol-execution path that this refocus explicitly DEFERS — building the flagship proof on deferred code is backwards. F3 is the only augmentation upstream structurally cannot ship (no browser device layer exists upstream), it directly serves product premise (b), it renders the confirmed-inert WebSerial bug as a visible badge instead of a buried RuntimeError, and it exercises all three adopted augmentation layers end-to-end, making it a proof of the API rather than a feature. F1 is DEFERRED but made cheap-by-construction: the transport's ring buffer (grafted from H) is exactly the data F1 needs, so F1 later becomes UI-only work. F2 (call provenance) shares F3's plumbing and is the natural second. F4 (click-to-code) is blocked on A4.
- [ACCEPT] PLR pin target for the visualizer work: 0.2.2 (dd79c4c89), not d9651e2 and not main: Spike-grounded on three counts: (1) set_root_resource shrinks 3x (485KB -> 164KB) because method signatures were hoisted into a per-class method_registry, which matters when every payload crosses a BroadcastChannel structured clone; (2) the unguarded `from pylibftdi import FtdiError` in the agilent plate_reading backend becomes a guarded try/except with HAS_PYLIBFTDI, which eliminates the entire reason the hand-built pylibftdi-0.0.0 stub wheel exists (debt-1289); (3) 0.2.2 renders byte-identically to main (396/220/18) so we lose nothing by not taking main. Also resolves debt-1291 favourably: at 0.2.2 there is NO pylabrobot.legacy and pylabrobot.liquid_handling IS the modern non-deprecated home — so honouring the user's prefer-modern directive means targeting 0.2.2, where the modern path and the working path are the same path. This is the honest reading of the directive: legacy.* is not needed because at this pin it does not exist.
- [DEFER] INVEST gate — "S (Small)" on the winner as scoped: FAILS as a single unit. The winner bundles five deliverables (vendoring generator, Python transport seam, JS augmentation API, F3 augmentation, three drift gates) plus an unvalidated load-bearing assumption (the Pyodide WebLoop shim), which is well past 8 acceptance criteria. Resolved by decomposition into four sequenced slices rather than by override: SLICE 0 (spike, blocking) — prove _PyodideLoopShim + BroadcastChannel + a scripted pick_up_tips end-to-end in a real Pyodide kernel, since pre-mortem FAILURE 1 makes this the single highest-value thing to learn and everything else is wasted if it is false; SLICE 1 — the V3 vendoring generator with D2 anchor assertions, producing a static visualizer page that renders a committed fixture payload with no Python at all; SLICE 2 — BrowserVisualizer + VisualizerTransport + ring buffer + D4 schema/sequence test, wired to slice 1; SLICE 3 — praxisViz augmentation API (A1/A2/A3) + F3 device-reality overlay + D3 nightly golden-render. Each slice is independently valuable and independently testable. The synthesis artifact is written for the whole design, but slices 0-1 are the only ones cleared to start.

## Assumptions

## TBDs

## Pre-mortem Record
**User:** PRE-MORTEM — six months on, this failed. The three most plausible stories, ranked:

FAILURE 1 (most likely) — "the deck renders once and never updates, and nobody noticed for weeks." The _PyodideLoopShim did not fully satisfy asyncio.run_coroutine_threadsafe's expectations under Pyodide's WebLoop. set_root_resource worked because it goes through the plain `await send_command` path, so the very first screenshot looked perfect and got demoed. But _handle_state_update_callback (visualizer.py:666) silently swallowed its exception inside a Resource state-update callback, so every pick_up_tips and aspirate updated nothing. Because upstream sends all four events with wait_for_response=False, there is NO ack path and NO error surfaced — the exact same silent-failure class as debt-1289 (wheel 404 -> praxis:ready still fires) and the WebSerial-inert bug. We shipped a third silent failure into a codebase already defined by them.
MITIGATION TO WRITE INTO THE SPEC: the loop shim must be validated by an EXECUTED spike inside a real Pyodide kernel before implementation starts, not after; every transport send must be counted and the count exposed as viz.stats(); and the D4 browserless test must assert on the EVENT SEQUENCE produced by a scripted pick_up_tips, not just on schema keys.

FAILURE 2 — "the augmentation API had exactly one user, forever." praxisViz.use/overlay/anchor was built, F3 shipped, and then nobody wrote a second augmentation, because the API was reachable only by editing a vendored file inside a generated-and-committed tree that CI rewrites on every submodule bump. Augmentations had nowhere to live that survived regeneration.
MITIGATION: the generator must emit a stable, NEVER-regenerated extension point — a praxis-owned augmentations/ directory outside the generated tree, loaded by a single <script type=module> the generator injects. If an augmentation cannot be added without touching a generated file, the API does not exist.

FAILURE 3 — "the vendored tree drifted three pins behind and re-vendoring became a project." Upstream moved the socket glue, D2's anchor assertion failed the build, and because re-anchoring required understanding vis.js, it got deferred; the pin froze; six months later the visualizer was 200 commits stale and the payload schema no longer matched anything.
MITIGATION: keep the patch count at FOUR anchors and treat growth in that number as a design regression that triggers escalating V4 (upstreaming a transport hook) rather than more patching. Also: D2's failure message must print the current upstream text around each failed anchor, so re-anchoring is a 5-minute edit rather than an investigation.

FOURTH, quieter failure worth naming: we chose 0.2.2, upstream's v1b1 reorg landed properly on main, and by month six 0.2.2 was the stale branch — so the pin decision that looked modern in August looks legacy by winter, and the prefer-modern directive was satisfied only momentarily. The spec should state the 0.2.2 choice as a DATED decision with an explicit re-evaluation trigger (the first tagged release containing pylabrobot.legacy), not as a permanent answer.
**AI:** _not recorded_

## Acceptance Criteria
**Given** FIXED CONSTRAINTS (cannot change):
- Static site only. No Python HTTP server, no websocket server, no backend. GH Pages is a target, so no COOP/COEP guarantee and no custom headers.
- The renderer runs in the browser main thread (a DOM document); the Python kernel runs in a DedicatedWorkerGlobalScope. `js.window` does NOT exist in the kernel — this is already a confirmed live bug in web_serial_shim.py. So ANY design that assumes the kernel can touch the visualizer DOM directly is dead on arrival. There must be a worker->document boundary crossing.
- lib.js is consumed unmodified (5853 lines). Non-negotiable.
- PLR is a git submodule; the visualizer assets must be derived from it, not hand-copied.
- Renderer and serializer must come from the SAME PLR pin (payload schema differs across pins).
- Konva 8.4.3 must be vendored; unpkg is not reachable in CI-like sandboxes.

NEW EVIDENCE I just gathered by reading external/pylabrobot/pylabrobot/visualizer/visualizer.py at the pinned d9651e2 — this materially changes option (1):
- `send_command(event, data, wait_for_response=False)` is the SINGLE funnel. All four events plus `show_machine_tools` and `stop` go through it (lines 604, 623, 626, 649, 658, 681, 585).
- Everything upstream of that funnel is reusable and NOT socket-aware: `_serialize_with_methods` (or `_serialize_resource_tree`+`_build_method_registry` at 0.2.2), `_sanitize_floats`, `_assemble_command` (adds id/version/data/event), the did_assign/did_unassign/state_update callback registration in `__init__` (lines 140-151), and the state-update BATCHING machinery `_enqueue_state_update`/`_flush_state_updates` (668-682) which exists precisely so a 96-channel pickup is one message.
- The ONLY socket-coupled bits are: `setup()` -> `_run_ws_server()`+`_run_file_server()`; `has_connection()`/`websocket` property; `stop()`'s httpd shutdown; and — this is the trap — three callbacks reach `self.loop` (`asyncio.run_coroutine_threadsafe(fut, self.loop)` at 650 and 659, `self.loop.call_soon_threadsafe` at 666). `self._loop` is set ONLY inside `_run_ws_server`, so a subclass that overrides only send_command still explodes on the first resource assignment.
- The fix is small and I want it on the table: in Pyodide an asyncio event loop IS already running, so a subclass can set `self._loop = asyncio.get_event_loop()` and `self.setup_finished = True` in its own setup(), and then `run_coroutine_threadsafe`/`call_soon_threadsafe` degrade to single-thread `call_soon` and just work. That means the override set is: `setup`, `stop`, `has_connection`, `send_command` — four methods, none of which mention a serializer, so the 485KB-vs-164KB payload-schema divergence across pins is INVISIBLE to us.
- Also note `_detect_source_filename()` runs in `__init__` and tries ipynbname/jupyter_server/urllib against a live Jupyter REST API — in Pyodide we must pass `name=` explicitly to short-circuit it.

NEGOTIABLE:
- Whether we subclass at all vs. reimplement the ~40 lines of callback wiring.
- Where the renderer lives: same-document sibling element, nested iframe, separate browser tab/window, or a notebook output cell (anywidget).
- Whether the channel is one-way (Python -> canvas) or bidirectional.
- Whether augmentations are authored in Python, in JS, or both.
- Which PLR pin we target (d9651e2 / 0.2.2 / main) — 0.2.2 currently looks strongly favoured by the 3x payload shrink and the pylibftdi stub going away.
- How much of the visualizer's own UI chrome (machine tools, sidebar, export) we keep.
**When** implementing A' — "BrowserVisualizer: thin subclass over a BroadcastChannel courier, into a vendored same-origin visualizer iframe, with a three-layer praxisViz augmentation API."

PYTHON SEAM: praxis/viz/browser.py defines BrowserVisualizer(pylabrobot.visualizer.Visualizer) overriding exactly four public/semi-public methods — setup(), stop(), has_connection(), send_command() — plus injecting a _PyodideLoopShim into self._loop. It never names a serializer, so the payload-schema split across pins is invisible to it. All serialization (_serialize_with_methods / _serialize_resource_tree + _build_method_registry), envelope assembly (_assemble_command: id/version/data/event), float sanitisation (_sanitize_floats), did_assign/did_unassign/state_update callback registration, and the 96-channel state-update BATCHING (_enqueue_state_update/_flush_state_updates) are inherited verbatim.

TRANSPORT: send_command() hands the assembled JSON string to an injected VisualizerTransport (Protocol, grafted from C) — BroadcastChannelTransport('praxis_viz') for the browser, plus RecordingTransport / FileTransport / NullTransport for tests. Every transport wraps a bounded ring buffer (grafted from H). The visualizer page announces readiness on the channel and the Python side answers by calling upstream's own _send_resources_and_state(), reusing the "ready" handshake rather than inventing one, which fixes BroadcastChannel's no-replay problem.

DELIVERY: a generated, committed praxis/web-client/src/assets/visualizer/ tree derived from the submodule at PLR 0.2.2 — stock lib.js/main.css/gif.js/gif.worker.js/img, a 4-anchor-patched vis.js, a de-templated index.html, and vendored konva.min.js 8.4.3. Rendered in a sibling same-origin iframe next to the JupyterLite iframe in the new static host page. lib.js is byte-identical to upstream.

AUGMENTATION API: three layers, all spike-verified, none touching lib.js internals — praxisViz.use(fn) middleware wrapping window.handleEvent/processCentralEvent (A1); praxisViz.overlay (a named Konva.Layer) plus praxisViz.anchor(resourceName) implementing the verified inverse-transform (A2); and praxis-namespaced side-channel events emitted from Python alongside the 4 stock ones (A3).

FIRST AUGMENTATION: F3 device-reality overlay. VENDORING: V3 generate-and-commit with a CI git-diff gate. DRIFT: D2 + D4 blocking per-PR, D3 nightly, D1 rejected.
**Then**
  - [ ] _add specific measurable criteria_
