---
session_id: b75b9e15
topic: Wheel build, PLR upgrade, and version coherence for the praxis JupyterLite REPL refocus: what mechanism makes version drift between the PLR submodule pin, the built browser wheel, the hardcoded TypeScript/Python filenames, and the jupyterlite piplite index structurally impossible; who builds the wheel and when (local dev vs CI vs committed artifact); what the three-Python policy should be (venv 3.14.6 / ty 3.12 / pyodide 3.13); and how to shape the PLR import surface so the next upstream reorg is localized.
task_type: architectural
winner: TWO DERIVED WHEELS, ONE MANIFEST, ONE FAIL-CLOSED BOOT (G + A-with-detector-inverted + D-redefined; axis 2 = F3 + F4-extended).

SIX PARTS.

(1) TWO WHEELS, BOTH DERIVED, NEITHER TRACKED. One recipe — `just browser-wheels`, invoked identically by CI — produces both artifacts into a gitignored praxis/web-client/src/assets/wheels/.
  (1a) pylabrobot: `git -C external/pylabrobot archive <pin> | tar -x -C $scratch`; in the SCRATCH COPY ONLY, overwrite pylabrobot/version.txt with `0.2.2+g<sha8>` (append `.dirty` if the submodule worktree is dirty), extend [tool.setuptools.packages.find] exclude with tests*/testing*/*_tests (86 of 513 files, ~1MB uncompressed), and inject pylabrobot/_praxis_build_info.py carrying PLR_SOURCE_SHA / PRAXIS_SOURCE_SHA / BUILD_ID; then `uv build --wheel`. The real repo and the real submodule are NEVER mutated. Zero network: the submodule is already vendored in every clone.
  (1b) praxis_browser: a real wheel built from praxis's OWN browser source — the four io shims, web_bridge, the single io-patching function, and the boot sequence. This is the part the prior spec did not have. Today those are seven loose .py files fetched over seven individual XHRs, each inside its own swallowing try/except (praxis_bootstrap.py:202-211), then exec'd into a throwaway namespace and stapled onto builtins (:229-236). Packaging them deletes seven silence points STRUCTURALLY — a zip installs or it does not — gives the shims one identity (killing the two-distinct-WebSerial-classes problem between the bootstrap's builtins-exec'd copies and pyodide_io_patch's module-level ones), makes debt-1290's ordering bug ordinary module code with an ordinary pytest, and lets the dead pyodide_io_patch.py be deleted rather than reconciled.

(2) THE PRIMARY DETECTOR IS AN INJECTED CONSTANT, NOT THE VERSION STRING. Runtime asserts `pylabrobot._praxis_build_info.PLR_SOURCE_SHA == manifest.pylabrobot.source_sha` — a plain string compare with zero PEP 440, URL-encoding or service-worker-cache-key exposure. The 0.2.2+g<sha8> version stamp is retained as SECONDARY, for filename disambiguation and cache invalidation, which is what it is genuinely good at, and is now droppable without losing the detector if any downstream tool rejects `+local`. Both signals are confirmed to reach the browser by direct read: __version__.py:6-8 opens version.txt at import time and pyproject.toml:56 ships it as package-data.

(3) MANIFEST.JSON IS THE ONLY SEAM. Emitted by the same recipe, in the same step, ATOMICALLY (temp + rename, so an interrupted build cannot leave a valid manifest describing an absent wheel). Schema per entry: {schema_version, package, filename, version, source_sha, sha256, bytes}. Entries for pylabrobot, praxis_browser, pylibftdi, and a vendored websockets. No consumer anywhere contains a wheel filename or version literal. praxis_bootstrap.py collapses to roughly: `_sync_fetch(manifest)` (its XHR helper at :14-22 already works in the worker) -> micropip.install each entry with deps=False -> `import praxis_browser` -> `await praxis_browser.boot()`. Enforcement is two-sided: gitignore the directory AND a CI check that no .whl and no manifest.json is git-tracked — without the second half this is a convention, and a hand-copied wheel is precisely how 7e29c68 created the original bug.

(4) BOOT FAILS BY RAISING IN THE CELL. praxis_browser.boot() is a staged pipeline; each stage declares required/optional and a postcondition predicate; results accumulate into a ledger. On any required postcondition failing it RAISES — JupyterLite renders a traceback in the cell output with zero infrastructure, and the kernel stays alive, so the user gets a specific actionable error AND a usable REPL. The BroadcastChannel emit (praxis:ready with the ledger, or praxis:error with the ledger) is demoted to a secondary machine-readable channel for CI and for any host that happens to exist. Two exception types, never one: PraxisDriftError (expected vs actual — never suppressible) and PraxisUnavailableError (404/network — loud, distinct, and the only thing an escape hatch may ever cover). Postconditions include: pylabrobot imports; _praxis_build_info.PLR_SOURCE_SHA matches; all four pylabrobot.io classes are the Web* shims and not the stock ones; WebSerial actually CONSTRUCTS (this fails on today's tree and is the point); navigator.serial/usb/hid present.

(5) IMPORT SURFACE. Delete _MACHINE_CLASS_MAP and create_machine_frontend (web_bridge.py:407-444) outright — every entry serves only the deferred catalog/runner, which the user has said may break, and deleting is cheap now and expensive later. praxis_browser becomes the single module in the system that names any pylabrobot import path, so the adapter exists for free. Add a declarative symbol contract — a data file enumerating every (module_path, symbol) the product depends on, covering BOTH praxis's own imports AND the pylabrobot imports appearing in shipped notebook templates and docs examples — plus a conformance test that imports every entry against the pinned wheel and fails on DeprecationWarning as well as ImportError. Ship the one-line fix regardless: Incubator moves from pylabrobot.incubator to pylabrobot.storage. Also fix web_serial_shim.py:24 by dropping `window` from the js import (it does not exist in DedicatedWorkerGlobalScope; this is why WebSerial has never worked) and add deps=False at python.worker.ts:406 and direct-control-kernel.service.ts:88 if those consumers survive, since 0.2.2 made websockets a mandatory dependency.

(6) PYTHON POLICY. Pin the KERNEL, derive the Python; no second component may pin it independently. In-browser CPython is currently 3.13.2 (MEASURED, not inferred) and is authoritative for all shipped Python. venv 3.14.6 is build-tooling-only and stays — a py3-none-any wheel is indifferent to its building interpreter — but must never be used to validate browser code; the in-browser Playwright smoke is authoritative by construction. ty's 3.12 target is below runtime, so false positives only, never false negatives: move it as a per-path override on the browser subtree, as cleanup. Do NOT pin pyodide to the 3.14 line — premature, not ours to pin, no gain on a version-agnostic wheel, and it would invalidate the only end-to-end green evidence we have. Revision trigger: jupyterlite-pyodide-kernel shipping a release whose pyodide is on 3.14. **[CORRECTED 260818]** The revision trigger already fired, unnoticed — exactly as FAILURE 5 below predicted. GATE G2 verified production now serves pyodide v314.0.1 / CPython 3.14.2, not the 3.13.2 this part measured (260817_g2-spike-battery-verdict.md §3). The "do NOT pin pyodide to 3.14" guidance is moot: the runtime is already on 3.14, unpinned by any repo config.

GRAFTED FROM LOSERS: B1 (PipliteAddon index -> piplite.install("pylabrobot") by bare name) is adopted as an opportunistic upgrade for the JupyterLite kernel path ONLY, gated on a spike proving PipliteAddon indexes a local wheel and that piplite's parser accepts a +local version; the manifest survives regardless for CI, diagnostics and non-piplite consumers. E's export-and-modify machinery is retained (it is required by part 1a anyway); E's patch series is not. C is rejected. F2 is absorbed into praxis_browser. Vendoring Pyodide to kill the cdn.jsdelivr.net dependency and the offline infinite-hang is sequenced as a separate tracked item gating the "offline-capable" product claim, and until it lands no doc may describe the site as self-contained.
created_at: 2026-08-17T15:30:01.537756+00:00
---

# Brainstorm: Wheel build, PLR upgrade, and version coherence for the praxis JupyterLite REPL refocus: what mechanism makes version drift between the PLR submodule pin, the built browser wheel, the hardcoded TypeScript/Python filenames, and the jupyterlite piplite index structurally impossible; who builds the wheel and when (local dev vs CI vs committed artifact); what the three-Python policy should be (venv 3.14.6 / ty 3.12 / pyodide 3.13); and how to shape the PLR import surface so the next upstream reorg is localized.

## Problem Frame
FRAME CONFIRMED as stated above. Restating tightly so it is on the record:

FIXED: pin dd79c4c89 (0.2.2); GH Pages static, no headers/no server logic; wheel is py3-none-any; micropip is the only installer and everything ends in a URL string; `jupyter lite build` is mandatory (tracked tree cannot serve); product = savable notebooks + device comms + visualizer; deferred paths may break.

NEGOTIABLE: wheel ownership and build timing; where (if anywhere) the filename lives; whether the two Angular Pyodide runtimes are in scope at all; whether the three-Python spread needs any action; the shape of the PLR import surface.

DRIVING ASYMMETRY: every failure mode here is silent-continue, and that is ONE pathology with five faces, not five bugs. Candidates get judged on whether they convert silence to noise, not just on whether they prevent drift.

MUST ANSWER: (a) detector + location for five drift scenarios; (b) who builds, and can an offline fresh clone deploy; (c) which Python is authoritative for what, using the MEASURED 3.13.2; (d) the import-surface rule that survives the NEXT pin bump.

Proceed to divergence.

## Idea Pool
- [user] I'm deliberately diverging on a DIFFERENT axis than the prior session used. That session diverged on "where does the wheel filename live" (manifest / codegen / PEP-503 index / glob / PyPI / gate). Re-running that axis would produce the same answer. The deeper axis is: WHAT IS THE UNIT OF COHERENCE — the thing that has one version identity and therefore cannot drift from itself? Six candidates, spread across three answers to that question, plus a separate axis for sub-problem (4).
- [user] === AXIS 1: WHAT IS THE UNIT OF COHERENCE? ===
- [user] --- ANSWER 1: THE ARTIFACT (wheel + descriptor). Runtime install stays; add data + assertions. ---
- [user] A. DERIVED SHA-STAMPED WHEEL + MANIFEST SEAM + POSTCONDITION GATE. The prior session's winner, corrected for new evidence. Build exports the pinned submodule to scratch, overwrites version.txt with 0.2.2+g<sha8>, excludes tests, `uv build --wheel` into a gitignored assets/wheels/, emits manifest.json alongside. Consumers read the manifest; zero filename literals. Bootstrap asserts pylabrobot.__version__ == manifest version. HOW IT WORKS: verified — __version__.py reads version.txt at import, version.txt is package-data, so the SHA is runtime-observable in the browser. COSTS: a new fetch (one more 404 surface), a manifest that can itself become a second source of truth, and a `+local` PEP 440 segment whose tolerance across micropip/piplite/service-worker cache keys is UNTESTED. FORECLOSES: nothing much — it is the conservative option. Its weakness is that it leaves the runtime-install architecture, and therefore the whole "network fetch at kernel boot" failure class, completely intact.
- [user] --- ANSWER 2: THE SITE (one build product, no runtime install at all). ---
- [user] B. PRE-INSTALLED PAYLOAD / NO MICROPIP CALL. Stop installing at runtime. Make the wheel part of the built site's package index so the kernel resolves `pylabrobot` by NAME, or go further and pre-materialize site-packages into the served tree. Concretely the ladder is: (B1) put the wheel in the piplite index via PipliteAddon so `piplite.install("pylabrobot")` works with no version and no filename — note jupyterlite-config.json:16 ALREADY declares piplite.packages:["pylabrobot"] and the spike proved it is inert, so this is finishing an abandoned intention, not inventing one; (B2) go to a custom pyodide lockfile so PLR loads as a kernel package at startup like numpy does; (B3) vendor pyodide itself into the tree so there is no CDN at all. HOW IT WORKS: `jupyter lite build` becomes the single build step producing a single versioned artifact — the site — and the wheel stops being an independently-addressable thing. COSTS: build complexity moves into jupyterlite addon mechanics I have not verified; larger critical-path download; `+local` version tolerance in piplite is the same untested risk as A and now load-bearing. FORECLOSES: the ability to hot-swap a wheel without a full site rebuild, and probably the second Angular runtime (python.worker.ts has no piplite).
- [user] C. VENDORED SOURCE, NO WHEEL AT ALL. PLR is pure Python and already in the repo as a submodule. Ship `external/pylabrobot/pylabrobot/` into the static tree — as a single .tar or .zip fetched and unpacked into the Pyodide VFS, with sys.path insertion — and delete the wheel concept entirely. HOW IT WORKS: coherence becomes a GIT property. There is no build recipe, no version.txt stamp, no filename convention, no micropip, no dist-info; `git submodule status` versus the deployed tarball is the whole story, and a CI check is a byte comparison. COSTS: loses importlib.metadata / dist-info (pylabrobot.__version__ still works since it reads version.txt directly, but anything doing importlib.metadata.version breaks); loses micropip's install semantics; the tarball needs a build step anyway to avoid 500 individual HTTP fetches, so "no build" is partly illusory. FORECLOSES: ever installing PLR the normal way in the browser, and any future use of extras/dependency resolution.
- [user] --- ANSWER 3: THE RUNNING KERNEL (accept the delivery mechanism, kill the silence). ---
- [user] D. FAIL-CLOSED STAGED BOOTSTRAP. Attack the pathology directly and structurally, independent of how the wheel arrives. Rewrite praxis_bootstrap's control flow: today it is 7+ independent try/except-and-continue blocks with an unconditional praxis:ready at the end that is downstream of nothing. Replace with an explicit stage table — each stage declares required/optional and a POSTCONDITION PREDICATE, results accumulate into a ledger, and the terminal emit is `praxis:ready` with the ledger IFF every required postcondition held, else `praxis:error` with the ledger. Postconditions are concrete and cheap: pylabrobot imports AND __version__ matches expected; all four io classes are the Web* shims not the stock ones; WebSerial actually constructs (which would have caught the never-worked serial bug); navigator.serial/usb/hid present. HOW IT WORKS: converts every one of the five faces of the pathology into a labelled loud failure at ONE checkpoint. COSTS: doesn't PREVENT anything; a hard gate invites a skip flag (the prior session's own pre-mortem said this is the most likely six-month failure). FORECLOSES: nothing; composes with A, B, C.
- [user] E. PRAXIS-OWNED PLR DISTRIBUTION (build-time patch series). Stop monkeypatching PLR at runtime. Apply a small patch series to the scratch export before building, so the io shims are REAL SOURCE inside the artifact rather than builtins-injected classes reassigned onto module attributes after import. HOW IT WORKS: one artifact whose identity covers PLR *and* our browser adaptations; kills the "two distinct WebSerial classes" problem and the _mock_native_deps-before-shim ordering bug structurally rather than by ordering discipline; makes the WebSerial `from js import window` bug a source fix in a place with a test. COSTS: a real patch series to rebase at every pin bump — the classic vendored-fork tax; and it makes our artifact NOT pylabrobot, which is a support/communication cost. FORECLOSES: naively swapping in an upstream PyPI wheel as a fallback.
- [user] === AXIS 2: THE IMPORT SURFACE (sub-problem 4) ===
- [user] Critically, my read narrowed this a lot: praxis_bootstrap touches ONLY pylabrobot.io.{serial,usb,ftdi,hid} and pylabrobot.resources — the two namespaces the spike proved are NOT deprecated even on main. The entire reorg-exposed surface is web_bridge.py:407-414 `_MACHINE_CLASS_MAP`, which is ALREADY a string-keyed lazy import table resolved by importlib.import_module at :442. So:
- [user] F1. DO NOTHING STRUCTURAL. Fix the one broken entry (Incubator -> pylabrobot.storage) and move on. The table already IS an adapter.
- [user] F2. NAMED ADAPTER MODULE. Create praxis_plr.py as the single place any praxis code names a pylabrobot module path; everything imports from it. Localizes the next reorg to one file by convention.
- [user] F3. AGGRESSIVE DROP. Delete _MACHINE_CLASS_MAP and create_machine_frontend outright — every entry serves the deferred catalog/runner, the user said breaking that is fine, and deleting is cheap now and expensive later. Reorg exposure goes to literally zero because the exposed surface ceases to exist.
- [user] F4. DECLARATIVE SYMBOL CONTRACT + CONFORMANCE TEST. A data file enumerating every (module_path, symbol) praxis depends on, plus a test that imports every entry against the pinned wheel and fails on any miss OR any DeprecationWarning. This is the only candidate that gives drift scenario [v] (silent namespace reorg) an actual DETECTOR rather than a hope.
- [user] I want to probe B and C hardest — they are the ones I have least evidence for and the ones that would change the shape most.

## Decision Log
- [REJECT] C. VENDORED SOURCE, NO WHEEL AT ALL — ship external/pylabrobot/pylabrobot/ as a tarball into the Pyodide VFS, delete the wheel concept: Fails on its own terms. Its sole advantage over A was "no build step," but 513 individual HTTP fetches is a non-starter so it needs a tarball, which needs a build step — the advantage collapses to "no setuptools." Against that it gives up dist-info/importlib.metadata, and critically it must hand-replicate setuptools package-data semantics: pyproject.toml:56 declares package-data `visualizer/*` and `visualizer/img/*`, meaning the PLR VISUALIZER'S BROWSER ASSETS SHIP INSIDE THE WHEEL and are a delivery vehicle for product deliverable (c). A hand-rolled tarball that silently drops them is a new silent failure inside a design whose entire purpose is eliminating silent failure.
- [DEFER] B1. PIPLITE INDEX as the core delivery mechanism — PipliteAddon indexes the local wheel, kernel calls piplite.install("pylabrobot") by name only: Elegant and strictly better than a manifest read IF it works — it deletes the filename AND the version from source, leaving only the package name. But it is not untried: jupyterlite-config.json:14-18 already declares piplite.packages:["pylabrobot"], and the spike proved it produced nothing (built pypi/all.json contains only ipykernel/piplite/pyodide-kernel/widgetsnbextension, no download attempt in the build log). Someone already reached for the elegant answer and it silently did nothing for the entire life of the file. Making the core mechanism the one mechanism in this codebase with a demonstrated history of silent no-op is unacceptable. Adopt as an opportunistic upgrade for the JupyterLite kernel path ONLY, gated on a spike proving (i) PipliteAddon actually indexes a local wheel from a federated_extensions/piplite_urls input and (ii) piplite's index parser accepts a PEP 440 +local version. Manifest survives regardless for CI, diagnostics, and any non-piplite consumer.
- [REJECT] E. PRAXIS-OWNED PLR DISTRIBUTION via a build-time patch series applied to PLR's own source: Rejected as a patch series, but its machinery is retained and its motivating problems are solved a cheaper way. The upstream-rebase tax is real and PLR is moving fast (a 776-file reorg landed 2 days after the pin we chose), so a patch series against upstream source is the most expensive possible way to carry our adaptations. However E's export-and-modify STEP is required regardless — the build must overwrite version.txt, extend the setuptools exclude, and inject _praxis_build_info.py — so that machinery is not incremental cost. The problems E was reaching for (two distinct WebSerial classes, the _mock_native_deps-before-shim ordering bug, untestable shims) are solved by candidate G instead, which patches OUR source into OUR wheel rather than upstream's into theirs, and therefore incurs no rebase tax.
- [ACCEPT] Primary drift detector: _praxis_build_info.PLR_SOURCE_SHA (a real module injected into the wheel) rather than the PEP 440 version stamp 0.2.2+g&lt;sha8&gt;: Inverts the prior spec's priority, which had the version stamp primary and the in-wheel marker as belt-and-braces. That ordering fails INTO the original bug: the +local segment is load-bearing but untested against micropip, piplite's index parser, importlib.metadata normalization, and service-worker cache keys where '+' is URL-significant. If any of them rejects it the pragmatic fix is "drop back to plain 0.2.2," which deletes the only discriminator between a pin-built and a main-built wheel (spike-proven: identical filename, 1,467,113 vs 1,816,152 bytes) while every surrounding check keeps reporting green because both sides read 0.2.2. A plain string constant in an injected module has zero PEP 440, URL-encoding or cache-key exposure. The version stamp is retained for what it is actually good at — filename disambiguation and cache invalidation — and is now droppable without losing the detector. Feasibility of the stamp path itself is separately confirmed by read: __version__.py:6-8 opens version.txt at import time and pyproject.toml:56 ships it as package-data, so both signals reach the browser.
- [ACCEPT] D redefined: the bootstrap failure surface is RAISING IN THE EXECUTING CELL, with the BroadcastChannel praxis:error emit demoted to a secondary machine-readable channel: D as originally stated ("emit praxis:error instead of praxis:ready") is disqualified: the praxis:ready/BroadcastChannel protocol exists only because Angular was listening, and this refocus removes Angular. The spike drove the REPL with nothing subscribed. Posting to a channel with no subscriber is exactly as silent as the try/except being replaced. praxis_main is awaited from an executing cell, so raising renders a traceback in the output area with zero infrastructure. It is also a SOFT hard-fail — the kernel stays alive, so the user gets a specific actionable error AND a usable REPL, which is a far weaker provocation for a skip-flag escape hatch than a bricked app (the prior session's own pre-mortem #1). Two exception types are mandatory: DRIFT (expected vs actual, never suppressible) and UNAVAILABILITY (404/network, loud, distinct, and the only thing an escape hatch may ever cover). Also record the honest limit: postconditions have the epistemic status of tests, not of build-time invariants. This is a very good smoke test, not a structural guarantee, and must not be described as the latter.
- [MERGE] Axis 2 import surface: F3 (delete _MACHINE_CLASS_MAP / create_machine_frontend) + F4 extended to cover shipped notebooks and docs examples, rejecting F1 and absorbing F2 into the praxis_browser wheel: F1 (just fix Incubator) is rejected: it leaves a live reorg surface for zero saving, and every entry in the table serves only the deferred catalog/runner, which the user has explicitly said may break. Deleting is cheap now and expensive later. F2 is not rejected but absorbed — candidate G makes praxis_browser the single module that names any pylabrobot import path, so the adapter exists for free and does not need to be a separate invented artifact. F3 alone is insufficient because it removes PRAXIS's reorg exposure but not the USER'S: in a REPL the user types `from pylabrobot.liquid_handling import LiquidHandler` in a cell, copied from OUR docs and OUR shipped notebook templates, and on any post-14a7766 pin that path emits a DeprecationWarning whose text instructs the user to import from pylabrobot.legacy. So F4's declarative symbol contract must enumerate praxis's own imports AND the pylabrobot imports appearing in shipped notebooks/templates/examples, and its conformance test must fail on DeprecationWarning as well as ImportError. That is the only detector proposed for drift scenario [v] that reaches the surface the product actually exposes.
- [ACCEPT] Three-Python policy: pin the KERNEL and derive the Python; venv 3.14.6 stays and is build-tooling-only; ty gets a per-path override; do NOT pin pyodide to the 3.14 line: The prior spec's Python section is void — it named "package.json pyodide ^0.29.0 (3.13 line) vs jupyterlite pyodide 314.0.1 (CPython 3.14.2)" as the one real incoherence, but the executed spike MEASURED the live JupyterLite kernel at pyodide 0.29.0 / CPython 3.13.2 with pylabrobot at /lib/python3.13/site-packages/. Both in-browser runtimes are on the SAME line; the incoherence does not exist and the prior spec solved a phantom. **[CORRECTED 260818]** This "solved a phantom" call has itself not survived: GATE G2 re-verified against the shipped kernel-extension schema and built bundle and found production now serves pyodide v314.0.1 / CPython 3.14.2, not the 0.29.0/3.13.2 this spike measured (260817_g2-spike-battery-verdict.md §3). The prior spec's original concern (package.json's `pyodide ^0.29.0` vs. jupyterlite's newer pyodide) was not a phantom after all — the two have simply since diverged further, to 0.29.0 vs. 314.0.1. Policy of record: (i) the in-browser CPython is DERIVED from the jupyterlite-pyodide-kernel pin, never chosen directly, and no second component may pin it independently; (ii) venv 3.14.6 is authoritative for build tooling only — a py3-none-any wheel is indifferent to its building interpreter — but must never be used to VALIDATE browser code, which is the in-browser Playwright smoke's job; (iii) ty's 3.12 target is BELOW runtime, so it yields false positives and never false negatives — safe-but-restrictive, move it as a per-path override on the browser subtree as cleanup, not as a fix; (iv) pinning pyodide to the 3.14 line is PREMATURE — it is not ours to pin, PLR's requires-python is >=3.9, the wheel is version-agnostic so there is no gain, and the only end-to-end green evidence in existence was produced on 3.13.2, so a bump would invalidate it for nothing. Revision trigger is jupyterlite-pyodide-kernel shipping a release whose pyodide is on 3.14 — not parity with the venv, which is an aesthetic.
- [ACCEPT] AMENDMENT (supersedes the F3 half of the axis-2 decision): MOVE _MACHINE_CLASS_MAP / create_machine_frontend into the experimental extension tree rather than DELETE them, with the Incubator fix applied in the same commit: Pre-mortem failure 3. Straight deletion is in tension with the stated product direction, which says the deferred scope is KEPT ALIVE, NOT DELETED. Worse, it creates a resurrection hazard: the fix (Incubator -> pylabrobot.storage) and the deletion land in the same commit, so anyone reviving the feature copy-pastes from the commit BEFORE it and reintroduces the exact broken `("pylabrobot.incubator", "Incubator")` entry the 0.2.2 bump invalidated. Moving achieves the identical goal — reorg exposure leaves the REPL payload, praxis_browser remains the single module naming pylabrobot paths — while making the revival path "use the thing sitting there" instead of "resurrect from history." Strictly better at zero extra cost.
- [ACCEPT] AMENDMENTS from pre-mortem: (i) one BUILD_ID stamped into BOTH wheels with boot asserting equality; (ii) AST-superset test guarding the symbol contract; (iii) declared-vs-observed in-browser Python asserted in CI; (iv) dev loop must be "rebuild the same artifact faster", never a second code path: Four gaps the winner had before the pre-mortem. (i) Two wheels create a NEW drift pair — praxis_browser built against a different pylabrobot sha — that the design assumed away with "one recipe builds both, so they are born coherent"; a partial rebuild or an incremental CI cache hit breaks that and every existing check still passes. A shared BUILD_ID fails on exactly the partial-rebuild mechanism, which sha cross-referencing does not. (ii) A hand-maintained symbol contract is structurally incapable of failing on an UNLISTED import, making it a check that cannot fail — the same pathology this whole design exists to kill. Machine-maintain coverage via an AST scan of the browser subtree and shipped notebook templates; hand-maintain only the expectations. (iii) Part 6's Python policy was the only part of the winner with no detector — "pin the kernel, derive the Python" works silently by design, so a routine jupyterlite-core bump moves the in-browser CPython with nothing to notice. Recording sys.version_info in the boot ledger and asserting it against a declared value in CI converts the policy from a document into an enforced invariant. (iv) The dev-mode escape is the winner's most fragile assumption and its own steelman's strongest attack: a loose-file dev path used daily by humans while CI tests only the wheel path guarantees divergence. The dev loop must be a faster rebuild of the SAME artifact, and if that loop cannot be made tolerable the two-wheel decision genuinely deserves re-litigating — that is stated as a live condition, not hand-waved.
- [MERGE] INVEST 'S' (Small) — winner as stated spans 6 parts plus 4 pre-mortem amendments, well past 8 acceptance criteria and more than one user-facing change: S FAILED as written. Resolved by SPLITTING, not overriding. CORE (this spec, 8 criteria, one coherent user-facing change — "the browser payload is a build product that fails loudly"): the two-target build recipe with SHA stamp + injected _praxis_build_info + shared BUILD_ID; atomic manifest emission; gitignore plus the no-tracked-artifact CI check; manifest-driven install with deps=False; the praxis_browser package holding the four shims, web_bridge and ONE io-patching implementation (deleting the dead pyodide_io_patch.py); the fail-closed staged boot raising in-cell with PraxisDriftError/PraxisUnavailableError; the Playwright smoke including its negative case; and the watch-mode dev loop — the dev loop is IN core, not deferred, precisely because the pre-mortem identified deferring it as the single most likely cause of total failure. SPLIT 1 (import surface): move _MACHINE_CLASS_MAP/create_machine_frontend to the experimental tree with the Incubator fix, plus the symbol contract, conformance test and AST-superset test — separable because it touches deferred scope and because the REPL's own PLR surface (io + resources) is reorg-immune, so it protects a FUTURE pin bump rather than this one. SPLIT 2 (Python policy): ty per-path override + declared-vs-observed in-browser Python CI assertion; pure cleanup plus one assertion, blocks nothing. ALREADY SEPARATE: the B1/piplite verification spike, and Pyodide vendoring / offline capability which gates the "offline-capable" product claim. With the split, CORE passes S.

## Assumptions

## TBDs

## Pre-mortem Record
**User:** PRE-MORTEM. Six months out, five failure stories. I deliberately avoided repeating the prior session's four (gate disabled / manifest as second truth / +local rejected / offline never finished) — those still apply and their mitigations are carried. These are failures specific to what THIS design added, and three of them changed the design.

FAILURE 1 (most likely) — THE DEV MODE BECAME THE REAL MODE. This is the steelman's first point coming true THROUGH its own mitigation. praxis_browser shipped with a dev escape: a URL param or a localStorage flag that makes the bootstrap fetch the loose .py files from assets/shims/ instead of installing the wheel, so hardware debugging keeps its edit-reload loop. Because WebSerial/WebUSB work can only be validated against physical hardware, every developer used the dev path every day, and the wheel path was exercised only in CI. Within months the two diverged: a shim fix landed in the loose file and the wheel's copy of the same module was subtly different, or an import that worked under exec-into-namespace failed under normal module import. The wheel path was "green in CI" the whole time because CI tested the wheel and humans tested the files, and neither tested what the other tested. MITIGATION THE SPEC MUST CARRY: the dev mode must not be a SECOND CODE PATH. It must be `micropip.install` of a locally rebuilt wheel via a watch task (`just browser-wheels --watch`), so the dev loop is "rebuild the same artifact faster," never "load the source a different way." If that loop cannot be made fast enough to be tolerable, the spec should say so and the two-wheel decision genuinely deserves re-litigating — this is the winner's single most fragile assumption.

FAILURE 2 — WE INVENTED A NEW DRIFT AXIS WHILE CLOSING AN OLD ONE. Two wheels means a pair that can disagree: praxis_browser built from praxis sha X assumes a pylabrobot API from sha Y. The manifest faithfully records both shas and nothing compares them, because the design assumed "one recipe builds both, so they are born coherent." Then someone rebuilt only one — a partial `just` invocation, an incremental CI cache hit, a developer regenerating one wheel while debugging — and shipped a praxis_browser whose io-patching code targeted module attributes that had moved. Every check passed: both wheels present, both shas recorded, both installed, pylabrobot's own SHA matched its manifest entry. MITIGATION, and it is cheap: stamp ONE BUILD_ID (uuid or timestamp+shas hash) into BOTH wheels' _praxis_build_info in the same invocation, and make boot assert BUILD_ID equality across the two. That is strictly better than cross-referencing shas because it fails on partial rebuilds, which is the actual mechanism. Adopt into part 2.

FAILURE 3 — THE DELETION CAME BACK WRONG. Part 5 deletes _MACHINE_CLASS_MAP and create_machine_frontend. Six months later someone reviving the experimental extension needed machine instantiation, found it in git history, and copy-pasted it back — complete with `("pylabrobot.incubator", "Incubator")`, the exact entry the 0.2.2 bump broke, because the fix and the deletion were in the same commit and the resurrection came from the commit BEFORE it. MITIGATION, and this is also a correction to my own decision: the product direction says the experimental scope is KEPT ALIVE, NOT DELETED. Straight deletion is in tension with that. The right move is MOVE, not DELETE — relocate _MACHINE_CLASS_MAP and create_machine_frontend into the experimental extension's own tree in the same commit, WITH the Incubator fix applied and with the symbol contract covering it. Then the revival path is "use the thing that is sitting there" rather than "resurrect from history," and the reorg exposure is still out of the REPL payload. Same benefit, no resurrection hazard. Amend part 5.

FAILURE 4 — THE SYMBOL CONTRACT ROTTED INTO DECORATION. It is a hand-maintained data file, and the conformance test only checks what is listed. Someone added a new pylabrobot import to praxis_browser and did not add a contract entry. The test stayed green — it is structurally incapable of failing on an unlisted import — and coverage decayed silently until the next reorg broke something the contract had never heard of. This is the same pathology as everything else in this area: a check that cannot fail is indistinguishable from no check. MITIGATION: the contract must not be purely hand-maintained. Add a test that AST-scans the browser subtree (and the shipped notebook templates) for every `pylabrobot.*` import and asserts the contract is a SUPERSET of what it finds. Hand-maintain the expectations; machine-maintain the coverage.

FAILURE 5 — THE PYTHON POLICY EXPIRED UNNOTICED. Part 6's revision trigger is "jupyterlite-pyodide-kernel ships a release whose pyodide is on 3.14." Nobody watches for that. A routine dependency bump moved jupyterlite-core, which moved the pyodide-kernel, which moved the in-browser CPython from 3.13 to 3.14 — and the policy that said "pin the kernel, derive the Python" worked exactly as designed, silently, which is the problem. Code authored against 3.13 either broke in a way blamed on something else, or subtly changed behaviour. MITIGATION that converts the policy from a document into an enforced invariant: the boot ledger must RECORD the observed sys.version_info and pyodide version, and the CI smoke must ASSERT them against a declared expected value in the repo. Then a kernel bump that moves the Python fails CI loudly and forces an explicit decision, instead of shipping. This also gives part 6 a detector, which it currently lacks — every other part of the winner has one.

Net: three amendments (BUILD_ID equality; move-don't-delete; AST-superset contract test), one new required detector (declared-vs-observed in-browser Python asserted in CI), and one honestly-flagged fragility (the dev loop) that is the winner's most likely undoing.
**AI:** _not recorded_

## Acceptance Criteria
**Given** FRAME CONFIRMED as stated above. Restating tightly so it is on the record:

FIXED: pin dd79c4c89 (0.2.2); GH Pages static, no headers/no server logic; wheel is py3-none-any; micropip is the only installer and everything ends in a URL string; `jupyter lite build` is mandatory (tracked tree cannot serve); product = savable notebooks + device comms + visualizer; deferred paths may break.

NEGOTIABLE: wheel ownership and build timing; where (if anywhere) the filename lives; whether the two Angular Pyodide runtimes are in scope at all; whether the three-Python spread needs any action; the shape of the PLR import surface.

DRIVING ASYMMETRY: every failure mode here is silent-continue, and that is ONE pathology with five faces, not five bugs. Candidates get judged on whether they convert silence to noise, not just on whether they prevent drift.

MUST ANSWER: (a) detector + location for five drift scenarios; (b) who builds, and can an offline fresh clone deploy; (c) which Python is authoritative for what, using the MEASURED 3.13.2; (d) the import-surface rule that survives the NEXT pin bump.

Proceed to divergence.
**When** implementing TWO DERIVED WHEELS, ONE MANIFEST, ONE FAIL-CLOSED BOOT (G + A-with-detector-inverted + D-redefined; axis 2 = F3 + F4-extended).

SIX PARTS.

(1) TWO WHEELS, BOTH DERIVED, NEITHER TRACKED. One recipe — `just browser-wheels`, invoked identically by CI — produces both artifacts into a gitignored praxis/web-client/src/assets/wheels/.
  (1a) pylabrobot: `git -C external/pylabrobot archive <pin> | tar -x -C $scratch`; in the SCRATCH COPY ONLY, overwrite pylabrobot/version.txt with `0.2.2+g<sha8>` (append `.dirty` if the submodule worktree is dirty), extend [tool.setuptools.packages.find] exclude with tests*/testing*/*_tests (86 of 513 files, ~1MB uncompressed), and inject pylabrobot/_praxis_build_info.py carrying PLR_SOURCE_SHA / PRAXIS_SOURCE_SHA / BUILD_ID; then `uv build --wheel`. The real repo and the real submodule are NEVER mutated. Zero network: the submodule is already vendored in every clone.
  (1b) praxis_browser: a real wheel built from praxis's OWN browser source — the four io shims, web_bridge, the single io-patching function, and the boot sequence. This is the part the prior spec did not have. Today those are seven loose .py files fetched over seven individual XHRs, each inside its own swallowing try/except (praxis_bootstrap.py:202-211), then exec'd into a throwaway namespace and stapled onto builtins (:229-236). Packaging them deletes seven silence points STRUCTURALLY — a zip installs or it does not — gives the shims one identity (killing the two-distinct-WebSerial-classes problem between the bootstrap's builtins-exec'd copies and pyodide_io_patch's module-level ones), makes debt-1290's ordering bug ordinary module code with an ordinary pytest, and lets the dead pyodide_io_patch.py be deleted rather than reconciled.

(2) THE PRIMARY DETECTOR IS AN INJECTED CONSTANT, NOT THE VERSION STRING. Runtime asserts `pylabrobot._praxis_build_info.PLR_SOURCE_SHA == manifest.pylabrobot.source_sha` — a plain string compare with zero PEP 440, URL-encoding or service-worker-cache-key exposure. The 0.2.2+g<sha8> version stamp is retained as SECONDARY, for filename disambiguation and cache invalidation, which is what it is genuinely good at, and is now droppable without losing the detector if any downstream tool rejects `+local`. Both signals are confirmed to reach the browser by direct read: __version__.py:6-8 opens version.txt at import time and pyproject.toml:56 ships it as package-data.

(3) MANIFEST.JSON IS THE ONLY SEAM. Emitted by the same recipe, in the same step, ATOMICALLY (temp + rename, so an interrupted build cannot leave a valid manifest describing an absent wheel). Schema per entry: {schema_version, package, filename, version, source_sha, sha256, bytes}. Entries for pylabrobot, praxis_browser, pylibftdi, and a vendored websockets. No consumer anywhere contains a wheel filename or version literal. praxis_bootstrap.py collapses to roughly: `_sync_fetch(manifest)` (its XHR helper at :14-22 already works in the worker) -> micropip.install each entry with deps=False -> `import praxis_browser` -> `await praxis_browser.boot()`. Enforcement is two-sided: gitignore the directory AND a CI check that no .whl and no manifest.json is git-tracked — without the second half this is a convention, and a hand-copied wheel is precisely how 7e29c68 created the original bug.

(4) BOOT FAILS BY RAISING IN THE CELL. praxis_browser.boot() is a staged pipeline; each stage declares required/optional and a postcondition predicate; results accumulate into a ledger. On any required postcondition failing it RAISES — JupyterLite renders a traceback in the cell output with zero infrastructure, and the kernel stays alive, so the user gets a specific actionable error AND a usable REPL. The BroadcastChannel emit (praxis:ready with the ledger, or praxis:error with the ledger) is demoted to a secondary machine-readable channel for CI and for any host that happens to exist. Two exception types, never one: PraxisDriftError (expected vs actual — never suppressible) and PraxisUnavailableError (404/network — loud, distinct, and the only thing an escape hatch may ever cover). Postconditions include: pylabrobot imports; _praxis_build_info.PLR_SOURCE_SHA matches; all four pylabrobot.io classes are the Web* shims and not the stock ones; WebSerial actually CONSTRUCTS (this fails on today's tree and is the point); navigator.serial/usb/hid present.

(5) IMPORT SURFACE. Delete _MACHINE_CLASS_MAP and create_machine_frontend (web_bridge.py:407-444) outright — every entry serves only the deferred catalog/runner, which the user has said may break, and deleting is cheap now and expensive later. praxis_browser becomes the single module in the system that names any pylabrobot import path, so the adapter exists for free. Add a declarative symbol contract — a data file enumerating every (module_path, symbol) the product depends on, covering BOTH praxis's own imports AND the pylabrobot imports appearing in shipped notebook templates and docs examples — plus a conformance test that imports every entry against the pinned wheel and fails on DeprecationWarning as well as ImportError. Ship the one-line fix regardless: Incubator moves from pylabrobot.incubator to pylabrobot.storage. Also fix web_serial_shim.py:24 by dropping `window` from the js import (it does not exist in DedicatedWorkerGlobalScope; this is why WebSerial has never worked) and add deps=False at python.worker.ts:406 and direct-control-kernel.service.ts:88 if those consumers survive, since 0.2.2 made websockets a mandatory dependency.

(6) PYTHON POLICY. Pin the KERNEL, derive the Python; no second component may pin it independently. In-browser CPython is currently 3.13.2 (MEASURED, not inferred) and is authoritative for all shipped Python. venv 3.14.6 is build-tooling-only and stays — a py3-none-any wheel is indifferent to its building interpreter — but must never be used to validate browser code; the in-browser Playwright smoke is authoritative by construction. ty's 3.12 target is below runtime, so false positives only, never false negatives: move it as a per-path override on the browser subtree, as cleanup. Do NOT pin pyodide to the 3.14 line — premature, not ours to pin, no gain on a version-agnostic wheel, and it would invalidate the only end-to-end green evidence we have. Revision trigger: jupyterlite-pyodide-kernel shipping a release whose pyodide is on 3.14. **[CORRECTED 260818]** The revision trigger already fired, unnoticed — exactly as FAILURE 5 below predicted. GATE G2 verified production now serves pyodide v314.0.1 / CPython 3.14.2, not the 3.13.2 this part measured (260817_g2-spike-battery-verdict.md §3). The "do NOT pin pyodide to 3.14" guidance is moot: the runtime is already on 3.14, unpinned by any repo config.

GRAFTED FROM LOSERS: B1 (PipliteAddon index -> piplite.install("pylabrobot") by bare name) is adopted as an opportunistic upgrade for the JupyterLite kernel path ONLY, gated on a spike proving PipliteAddon indexes a local wheel and that piplite's parser accepts a +local version; the manifest survives regardless for CI, diagnostics and non-piplite consumers. E's export-and-modify machinery is retained (it is required by part 1a anyway); E's patch series is not. C is rejected. F2 is absorbed into praxis_browser. Vendoring Pyodide to kill the cdn.jsdelivr.net dependency and the offline infinite-hang is sequenced as a separate tracked item gating the "offline-capable" product claim, and until it lands no doc may describe the site as self-contained.
**Then**
  - [ ] _add specific measurable criteria_
