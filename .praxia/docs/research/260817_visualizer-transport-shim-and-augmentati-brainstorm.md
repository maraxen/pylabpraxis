---
session_id: 1b91bd39
topic: Visualizer transport shim and augmentation API for the praxis JupyterLite REPL refocus: how to drive the PyLabRobot Konva visualizer from an in-browser Pyodide kernel with no websocket server, and what shape the augmentation API should take, given proven-by-execution spike results (renderer is a pure function of injected JSON; 4 vis.js spans are the entire network coupling; a second Konva overlay layer attaches with zero lib.js edits).
task_type: architectural
winner: TWO-TIER WRAP. Python transport seam = duck-typed sink injected into an otherwise-untouched upstream Visualizer, plus a 3-method subclass replacing only the asyncio-welded callbacks (PraxisBrowserVisualizer). JS transport seam = upstream vis.js vendored and patched at 4 asserted anchors, replacing the websocket with a postMessage receiver. Augmentation API = two explicitly versioned tiers: Tier 1 middleware over the wire envelope (stable, bidirectional, drawing-incapable), Tier 2 canvas access through a single narrow lib-adapter (privileged, may break on PLR bumps). Vendoring = hash-pinned V1 script in CI vendoring upstream JS + konva.min.js, jszip deleted, html2canvas/gif.js lazy, bootstrap restyled out. Drift detection = three complementary detectors: D2 anchor assertions at patch time, D4 source-hash sentinels on the shadowed Python methods at unit-test time, D1 golden-render contract test at CI render time. First augmentation = F3 click-to-Python, chosen to retire the unproven upward-path risk; F1 state-diff highlight second.
created_at: 2026-08-17T14:39:47.517814+00:00
---

# Brainstorm: Visualizer transport shim and augmentation API for the praxis JupyterLite REPL refocus: how to drive the PyLabRobot Konva visualizer from an in-browser Pyodide kernel with no websocket server, and what shape the augmentation API should take, given proven-by-execution spike results (renderer is a pure function of injected JSON; 4 vis.js spans are the entire network coupling; a second Konva overlay layer attaches with zero lib.js edits).

## Problem Frame
FIXED (cannot change):
- F1. The renderer stays a vendored third-party artifact. lib.js is 5867 lines of Konva code we do not own, do not understand in detail, and will re-vendor from upstream repeatedly. Zero edits to it. Non-negotiable.
- F2. No websocket server, no localhost:2121/:1337. Everything runs in the browser. The Python side is a Pyodide kernel in a Web Worker; the renderer is DOM-side. They are separated by a worker boundary (structured clone), not a socket.
- F3. The page must be servable as static files. No server-side rendering, no build-time-only-in-Angular tricks. GH Pages is the deploy target.
- F4. PLR's asyncio callback plumbing (visualizer.py:140-151, :628-682) cannot be used as-is — self.loop.call_soon_threadsafe blows up without a running loop (proven: spot.tracker.commit() -> 'Event loop has not been started.').
- F5. The wire format (STANDARD_FORM_JSON_VERSION 0.1.0, the {event, id, data} envelope, set_root_resource/resource_assigned/resource_unassigned/set_state) is upstream's, not ours. We consume it; we do not get to redesign it.

NEGOTIABLE:
- N1. Whether we vendor the PINNED submodule's visualizer JS or UPSTREAM master's. Spike says upstream is strictly better (gridLayer, visibility toggles, method_registry cutting 461KB -> 118KB) and is backward-compatible with an un-bumped 0.1.6 kernel via lib.js:967-968's `resourceData.methods || methodRegistry[resourceData.type]` precedence.
- N2. Whether the Python side uses PLR's Visualizer class at all, or bypasses it and calls the pure serializers directly (_serialize_with_methods, _sanitize_floats, _assemble_command — proven callable with no server).
- N3. Whether vis.js is patched, replaced wholesale, or shimmed from outside without touching the file.
- N4. Where the visualizer lives relative to the notebook: separate iframe, sibling pane, or embedded output cell.
- N5. Whether augmentations are Python-authored, JS-authored, or both.
- N6. How much of upstream's GIF/export UI we keep.

SHARPEST TENSION I want the divergence to attack: criterion A (drift resilience) and criterion D (augmentations survive lib.js INTERNAL refactors) pull in opposite directions from criterion E (small enough to ship). The cheapest augmentation seam is the one the spike proved — reach into lib.js's `resources[name].group` and `stage` globals — but that is precisely a dependency on lib.js INTERNALS, which is the thing criterion D says must not break us. I want at least one candidate that refuses that shortcut and pays a different price.

HIDDEN ASSUMPTION I want surfaced: everyone assumes the visualizer is a passive display. The user asked for "augmentations", which may imply INPUT — clicking a well to get a Python handle, dragging a plate to reposition it. If augmentations are bidirectional, the transport seam is not one-way and several otherwise-attractive designs die.

## Idea Pool
- [ai] T1 — DUCK-SOCKET INJECTION. Keep upstream's Visualizer class byte-intact and unsubclassed. Never call setup(). Instead assign two private attributes: viz._websocket = _BrowserSink() (an object whose only method is `async def send(self, s: str)`, forwarding to JS) and viz._loop = asyncio.get_event_loop() (pyodide's live WebLoop). Then call viz._send_resources_and_state() once, in place of the browser's "ready" handshake. Everything else — id generation, _sanitize_floats, _serialize_with_methods, the three resource callbacks, the set_state batching/flush, show_machine_tools — executes upstream's own code path unmodified. Drift surface = 3 private names (_websocket, _loop, _send_resources_and_state) and the duck-type contract `async send(str)`.
- [ai] T2 — SUBCLASS + OVERRIDE THE ASYNC BOUNDARY. class BrowserVisualizer(Visualizer): override setup() (no servers; just mark setup_finished and push the initial snapshot), send_command() (assemble via super()._assemble_command, then postMessage; never wait for response), and stop(). Uses the named, documented-ish method surface rather than private attributes. Drift surface = the signatures of setup/send_command/stop plus __init__'s constructor params (host, ws_port, fs_port, open_browser, name, favicon, show_machine_tools_at_start — already 7 and growing).
- [ai] T3 — OWN THE PUMP, BORROW ONLY THE PURE FUNCTIONS. Do not instantiate Visualizer at all. praxis/visualizer/pump.py imports only _serialize_with_methods and _sanitize_floats (pure, proven callable serverless) and re-implements ~120 lines: register_did_assign_resource_callback / register_did_unassign_resource_callback / register_state_update_callback against a SYNCHRONOUS sink, its own id counter, its own microtask-based batching. Zero asyncio. Drift surface moves OFF visualizer.py (where the 32 commits are) and ONTO Resource's callback API + the wire format. Cost: we own the pump and must manually track new event types upstream adds (show_machine_tools and method_registry were both added in the delta).
- [ai] T4 — JUPYTER-NATIVE TRANSPORT (comm / _repr_mimebundle_). Ship the visualizer as a notebook output: a PraxisVisualizer object whose _repr_mimebundle_ emits an iframe/srcdoc carrying vendored lib.js+konva, and whose message channel is Jupyter's own comm protocol (or an anywidget-style widget) rather than a bespoke postMessage bridge. Transport becomes "whatever JupyterLite already implements for kernel<->frontend messaging". Materially different in WHERE the seam lives: not our bridge, but the notebook protocol. Consequence: the visualizer lives in an output cell, is savable/serializable with the notebook, and multiple visualizers can coexist per notebook.
- [ai] T5 — WEBSOCKET POLYFILL (keep upstream 100% untouched, both sides). Install a fake `websockets` module into the Pyodide kernel and a fake `WebSocket` global in the visualizer document, both backed by postMessage. Then upstream's Visualizer.setup() and upstream's unmodified vis.js openSocket() both "work" verbatim — zero patches to vis.js, zero patches to visualizer.py, zero private-attribute access. Maximum drift immunity by construction: we impersonate the protocol boundary instead of bypassing it. Cost: must also fake http.server/threading/webbrowser for setup()'s _run_file_server, and impersonating `websockets.asyncio.server.serve` is a nontrivial API surface.
- [ai] A1 — OVERLAY LAYER (JS-side, draws on top). The spike-proven seam: stage.add(new Konva.Layer({name:'praxis-overlay'})), positioned via resources[name].group.getClientRect({relativeTo: stage}). Augmentations are JS functions receiving (overlayLayer, resources, stage). Requires the scaleY(-1) counter-flip for text. Depends on lib.js INTERNAL globals: `stage`, `resources`, and each entry having a `.group` Konva node.
- [ai] A2 — MESSAGE-BOUNDARY MIDDLEWARE (wire-format-only dependency). An interceptor chain wrapping the inbound envelope before it reaches processCentralEvent. Each augmentation is `(envelope, next) => ...` and may: rewrite the payload, consume a praxis:* synthetic event (MANDATORY — vis.js:112-113 throws on unknown events), or pass through. Depends ONLY on the {id, version, event, data} envelope and the 5 known event names — the wire format that held stable across a 222-commit delta (STANDARD_FORM_JSON_VERSION 0.1.0 on both sides). Zero dependency on lib.js internals. Cannot draw anything by itself.
- [ai] A3 — PYTHON-SIDE TREE ENRICHMENT (zero JS). Augmentations are Python decorators that rewrite the serialized dict before send — recolor a well by editing its state, inject a synthetic child Resource to act as a marker, annotate `methods`. Advantage: augmentation authors write Python, never touch JS, and everything is testable headlessly with no browser. Disadvantage: strictly limited to what lib.js ALREADY knows how to draw; you are programming the renderer through a keyhole, and injecting fake Resources pollutes the tree that the sidepanel/Workcell Tree also renders.
- [ai] A4 — SIDECAR CANVAS / SHADOW STAGE. Refuse to touch lib.js's stage at all. Mount a SECOND, fully independent Konva stage (or plain 2D canvas / absolutely-positioned DOM layer) in a sibling element stacked over the visualizer div, and keep it in sync by maintaining our OWN geometry model derived from the wire payload (every resource's location/size is in the set_root_resource JSON). Pays a real price — we must re-derive the world->screen transform and mirror pan/zoom ourselves — to buy total independence from lib.js internals. This is the candidate that refuses the internals shortcut.
- [ai] A5 — BIDIRECTIONAL / INPUT AUGMENTATIONS. Augmentations emit upward: overlay-layer or stage click handlers post {event:'praxis:pick', resource_name} back to the kernel, which resolves it to a live Python object and (e.g.) binds it to a variable, prints a repr, or triggers a protocol step. Turns the visualizer from a display into an input device. Forecloses any design where the transport is one-way; requires an inbound queue on the Python side and a story for "kernel is busy running a cell".
- [user] Recorded 10 so far across two axes. Transport: T1 duck-socket injection, T2 subclass+override, T3 own-the-pump, T4 Jupyter-native comm/mimebundle, T5 websocket-polyfill-both-sides. Augmentation: A1 overlay layer, A2 message-boundary middleware, A3 Python tree enrichment, A4 sidecar shadow canvas, A5 bidirectional input.
- [user] Third axis still to diverge (vendoring + drift detection), and I want to push on orthogonality first. My first instinct was T1+A1 — the cheapest pair, both essentially "reach into what the spike already proved". Deliberately arguing against myself:
- [user] The A1/A4 split is the real one. A1 is 20 lines and depends on three lib.js globals (`stage`, `resources`, `.group`). A4 is maybe 300 lines and depends on nothing but the JSON. Criterion D says augmentations must survive lib.js INTERNAL refactors, and `resources[name].group` is exactly an internal.
- [user] But there is a third position neither covers: make the internals dependency EXPLICIT AND TINY rather than either accepting it diffusely or paying 300 lines to avoid it. One ~15-line adapter module whose entire job is `getStage()`, `getResourceNode(name)`, `getResourceRect(name)` — and which is the ONLY file in the repo permitted to reference a lib.js global. Everything else programs against that interface. Then drift costs one file, and a contract test can assert those three functions still return sane values.
- [user] Also unresolved: T4 is not merely a transport variant, it changes WHERE the visualizer lives (output cell vs iframe pane) and therefore what "savable notebook" means for a rendered deck. That is a product question hiding inside a transport question.
- [user] Probe me on: (a) whether T1's private-attribute dependency is actually worse than T2's public-signature dependency given that upstream is ACTIVELY refactoring — which surface really moves more? (b) whether T5's "impersonate the protocol" is genuine drift immunity or just relocated coupling. (c) what A2 can and cannot do alone, since it draws nothing.
- [ai] A6 — NARROW ANTICORRUPTION ADAPTER (third position between A1 and A4). Accept the lib.js-internals dependency but confine it to ONE tiny file, praxis/visualizer/js/lib-adapter.js, exporting exactly three functions: getStage(), getResourceNode(name), getResourceRect(name). It is the only file in the repo permitted to reference a lib.js global. Every augmentation programs against that interface. Drift then costs one file to repair, not N augmentations, and a contract test can assert the three functions return sane values against a freshly vendored lib.js. Buys ~90% of A4's resilience for ~5% of A4's code.
- [ai] V1 — VENDOR-BY-SCRIPT WITH PINNED HASHES. A committed tools/vendor_visualizer.py that copies lib.js/vis.js/main.css/index.html/img from external/pylabrobot at the recorded submodule SHA, downloads konva.min.js from raw.githubusercontent.com/konvajs/konva/8.4.3/konva.min.js verifying sha256 bcf15fc061be942780cfd3a42a0d451fa91cc4f463e561a9f13b43a43d88e2fb, drops jszip entirely (zero refs in lib.js), and writes a vendor.lock.json recording every source SHA. CI re-runs it and fails on diff. Answers 'who rebuilds them': the same recipe that builds the wheel, in CI, never a human.
- [ai] V2 — NPM/ESM BUILD PIPELINE. Add konva as a real package.json dependency and bundle it with the existing Angular/esbuild toolchain. Reuses machinery that already exists, gets dependabot updates for free. But: couples the static-page build to the Node toolchain and to the Angular workspace we are trying to decouple from, and konva@8 UMD is what lib.js expects as a global — an ESM bundle needs a shim to expose window.Konva.
- [ai] D1 — GOLDEN-RENDER CONTRACT TEST (drift detection). Commit the captured 461KB set_root_resource + the set_state delta as golden fixtures. CI runs headless Chromium against the vendored bundle, injects the fixtures, and asserts a structural invariant set — resourceKeys==208, resourceLayerDescendants>600, rootResourceName=='deck', treeNodes>0, console.errors==0, requestfailed==0 — plus the adapter contract (getStage() returns a Konva.Stage, getResourceRect('tips_01') returns a finite non-zero rect). Runs on every vendor bump. Detects silent render breakage BEFORE merge. Cost: CI needs unsandboxed headless Chromium and a pre-cached browser binary.
- [ai] D2 — ANCHOR-ASSERTION PATCHER (drift detection at patch time). Rather than editing vendored vis.js by hand, apply the patch programmatically: the vendor script asserts each of the 4 anchor strings (webSocket.send(JSON.stringify(ret)), function openSocket(), function heartbeat(), the load listener) matches EXACTLY ONCE, and hard-fails the build if an anchor matches 0 or 2+ times. Proven viable — the spike's patch_harness.py did exactly this and applied cleanly to BOTH pinned and upstream trees with all 4 anchors asserted. Turns a silent semantic break into a loud build failure at the earliest possible moment. Complements D1: D2 catches vis.js drift at patch time, D1 catches lib.js drift at render time.
- [ai] D3 — NO PATCH AT ALL: SHADOW vis.js. Do not vendor upstream's vis.js. Write praxis-vis.js from scratch (~60 lines) that provides the same five global entry points lib.js expects, and simply never load upstream's vis.js. Since vis.js is only 212 lines and is ENTIRELY transport+status-label (setRootResource/setState/removeResource/snapshotResource/processCentralEvent/handleEvent are all thin dispatchers), replacing it wholesale removes the patch-drift problem by removing the patch. Risk: we must track upstream's additions to processCentralEvent (show_machine_tools was added in the delta) — D1's golden render is what catches that.
- [ai] F1 — FIRST AUGMENTATION: TIME-TRAVEL / STATE-DIFF HIGHLIGHT. Route web_bridge's existing per-call state_before/state_after capture to the canvas: after each traced PLR call, flash an overlay outline on exactly the resources whose serialized state changed, with a scrub-back control. Exercises the full stack — Python capture -> synthetic praxis:* event -> middleware interception -> overlay draw keyed by resource name. Strongest API proof because it touches every layer. Risk: depends on debug-panel plumbing whose quality is unaudited, and 'time travel' implies replay/undo semantics we have not designed.
- [ai] F2 — FIRST AUGMENTATION: LIVE CHANNEL/TIP TRACE. Draw the pipetting head position and per-channel aspirate/dispense arrows on the overlay as commands execute. Highest demo value for a lab-automation audience, directly legible. But it needs LiquidHandler backend hooks (pylabrobot.legacy.* after the v1b1 move — debt-1291 territory), so it couples this design area to the wheel bump we explicitly declared out of scope.
- [ai] F3 — FIRST AUGMENTATION: CLICK-TO-PYTHON ('pick a resource'). Click any resource in the canvas; the kernel binds it to a variable and echoes `plate = deck.get_resource("plate_01")  # <Plate plate_01>` into the REPL. Smallest end-to-end proof that exercises the RETURN direction (A5), which is the direction most likely to be under-designed and the one that forecloses the most if discovered late. Also the highest ratio of product value to lines of code for a notebook-centric product: it closes the loop between 'I see it' and 'I can script it'.
- [ai] F4 — FIRST AUGMENTATION: NONE. Ship the transport shim with a documented but EMPTY augmentation registry plus one deliberately trivial reference augmentation (a static 'praxis' watermark / a bounding box around a named resource) that exists only as an executable example and CI fixture. Defers the question of which augmentation is valuable until the API has real users. Cheapest, and least likely to bake a wrong abstraction — but produces no user-visible win and gives the API no real stress test.
- [user] 26 recorded across four axes (transport T1-T5, augmentation shape A1-A6, vendoring/drift V1-V2 + D1-D3, first-augmentation F1-F4). Answering my own probes, and surfacing what I think is the session's key reframe:
- [user] (a) T1-private vs T2-public — which surface actually moves more? I read the class. `send_command`'s signature has grown a `wait_for_response` kwarg and `__init__` now carries 7 params including two added in the delta (favicon, show_machine_tools_at_start). Meanwhile `_websocket` and `_loop` are structural — they are what the class IS. My read is the conventional intuition inverts here: the public constructor and setup() surface is the churny part (it is where features land), and the private attributes are the stable part (they are the machine). But this is an argument, not evidence.
- [user] (b) T5 'impersonate the protocol' — I now think this is FALSE immunity. To make setup() work I must fake `websockets.asyncio.server.serve` as an async context manager, `http.server.HTTPServer`, `threading.Thread`, and `webbrowser.open`. That is a LARGER and less stable surface than the 3 private names T1 touches, and it buys nothing the duck-socket does not already buy. I want to reject it, but not before someone argues for it.
- [user] (c) A2 draws nothing — correct, and that is the point. A2 is not a rival to A1/A4/A6; it is the LAYER BELOW them. Every augmentation needs to know WHEN to act, and A2 is the only 'when' that does not depend on lib.js internals.
- [user] THE REFRAME: I have been treating these as four independent choices, and I now think that is wrong. The real structure is a two-tier API, and the tiers have DIFFERENT drift exposures, so they should be specified separately and versioned separately:
- [user] Tier 1 (stable, wire-format-only): observe/transform messages. Anyone can write these. Survives any lib.js refactor. = A2 + A3.
- [user] Tier 2 (privileged, internals-dependent): draw on the canvas, hit-test, read geometry. Goes through ONE adapter. = A1 confined by A6.
- [user] That reframe converts 'A1 vs A4 vs A6' from a competition into a layering, and it makes the honest statement to augmentation authors: 'Tier 1 will not break; Tier 2 may break on a PLR bump, and here is the one file that fixes it.'
- [user] Divergence feels near exhaustion — the last several ideas are refinements rather than new directions, and I have at least three materially different positions on every axis. Ready to converge.

## Decision Log
- [REJECT] T5 — Websocket polyfill on both sides (impersonate the protocol boundary): False immunity. To let upstream's setup() run verbatim we must impersonate websockets.asyncio.server.serve as an async context manager, http.server.HTTPServer, threading.Thread and webbrowser.open — a LARGER and less stable surface than the 3 private names T1 touches, and one that is stdlib/third-party-version-sensitive rather than PLR-version-sensitive. It relocates coupling from a surface we can contract-test (PLR internals, one repo, one pinned SHA) to one we cannot (the websockets library's server API). Buys nothing the duck-socket does not already buy.
- [REJECT] V2 — NPM/ESM build pipeline for Konva: Couples the static-page build to the Node/Angular workspace we are explicitly decoupling from (product direction: Angular deferred to the experimental extension). Also a mechanical mismatch: lib.js expects a UMD global window.Konva, so an ESM bundle needs an extra shim. V1's hash-pinned fetch from raw.githubusercontent.com is proven to work in this environment (200, 158,587 bytes, sha256 verified) and produces a byte-equivalent artifact to what unpkg serves for konva@8. Fewer moving parts, no Node on the critical path.
- [REJECT] D3 — Shadow vis.js (rewrite rather than patch): Conceptually cleanest but fails silently in the one way we cannot afford. vis.js is 212 lines and looks like pure dispatch, but it carries at least two non-obvious behaviours: snapshotResource() MUST run before removeResource() so the machine-tool arm panel can replicate a destroyed resource (the file carries a comment saying exactly this), and the set_state branch calls updateSidepanelState() per resource to avoid a full tree rebuild. Rewriting means silently dropping features we do not use TODAY and will not notice losing. D2's anchor-asserted patch keeps upstream's semantics byte-for-byte outside the 4 replaced spans and converts drift into a loud build failure instead of a silent behaviour loss.
- [DEFER] A4 — Sidecar shadow canvas (independent second Konva stage, own geometry model): Not rejected on merit — it is the only candidate with genuinely zero dependence on lib.js internals, and it is the right escape hatch if PLR's renderer churn becomes intolerable. But it requires re-deriving the world->screen transform and mirroring pan/zoom, and the spike proved the CHEAP path works: an overlay layer added to lib.js's own stage stayed locked to resources through a 2.6x zoom with zero lib.js edits. A6's narrow adapter buys most of A4's resilience for ~5% of the code. Deferred with a named trigger: adopt A4 if the A6 adapter needs repair on more than two consecutive PLR bumps.
- [MERGE] T1 + T2 + T3 merged: duck-socket injection, plus a minimal subclass overriding ONLY the three asyncio-welded callbacks: Pure T1 rests on an unverified claim — that pyodide's WebLoop satisfies call_soon_threadsafe (visualizer.py:666), call_soon (:673), asyncio.run_coroutine_threadsafe (:650, :659) and ensure_future (:682). Faking the loop instead means impersonating asyncio internals (_chain_future, get_loop, add_done_callback), which is CPython-version-sensitive — worse than the disease. The merge dodges both: duck-type _websocket (T1's insight — the websocket property is only ever `self._websocket` and has_connection() is `is not None`, so any object with `async def send(str)` satisfies it), and use T2's subclass mechanism to override exactly the three methods the spike PROVED are non-reusable (_handle_state_update_callback, _handle_resource_assigned_callback, _handle_resource_unassigned_callback), replacing asyncio dispatch with a synchronous send. This adopts T3's synchronous-pump philosophy for the ~25 lines that need it, while _assemble_command, _serialize_with_methods, _sanitize_floats, _generate_id, send_command and _send_resources_and_state all still run upstream's unmodified code. Critically: every one of those callback sites calls send_command with wait_for_response=False, so the coroutine never truly suspends and a one-step drive-to-completion runner is sufficient — and a loud RuntimeError if it ever DOES suspend becomes a free drift detector.
- [ACCEPT] D4 (new, emergent) — Source-hash sentinel test over the shadowed upstream methods: Fills the gap the merge opens. Once we override three upstream methods, upstream can change what data dict resource_assigned carries and our override goes stale SILENTLY — the exact failure class (silent, partial) that the wheel-filename bug and the WebSerial `window` import already demonstrated in this codebase. Mitigation is cheap and needs no browser: a plain unit test asserting sha256(inspect.getsource(Visualizer._handle_resource_assigned_callback)) and its two siblings match recorded values, plus the three pure functions we import. Fails loudly on the exact methods we shadowed, on the exact bump that changes them, in CI, in milliseconds. Complements D2 (vis.js anchors, patch time) and D1 (golden render, render time) — three detectors covering three distinct drift surfaces: Python semantics, JS transport, JS renderer.
- [ACCEPT] A2 + A6 two-tier augmentation API (Tier 1 wire-format middleware; Tier 2 canvas access via one narrow adapter): Resolves the session's sharpest tension (drift resilience vs shippability) by layering rather than choosing. Tier 1 depends only on the {id, version, event, data} envelope and the 5 known event names — a surface demonstrably stable across a 222-commit delta (STANDARD_FORM_JSON_VERSION 0.1.0 on both pinned and upstream, and 0.1.6-generated payloads render unmodified on the upstream renderer). Tier 2 concentrates ALL lib.js-internals coupling (`stage`, `resources`, `.group`) into one ~40-line adapter file that is the only place in the repo permitted to name a lib.js global. This lets us make an honest, differentiated stability promise to augmentation authors instead of one vague one. A3 (Python tree enrichment) becomes a Tier-1 variant that happens to run kernel-side; A5 (bidirectional) becomes Tier 1's return channel rather than a separate concept.
- [ACCEPT] F3 (click-to-Python) as the FIRST augmentation, ahead of F1 (time-travel state-diff highlight): Chosen on risk retirement, not on demo value. F1 exercises only the downward path, which both spikes already proved works (injected JSON renders; set_state deltas repaint). F3 exercises the UPWARD path, which nothing has proved and which forecloses the most if discovered late — a one-way transport is materially cheaper and we would be tempted into it. F3 also forces us to answer 'what happens when the user clicks while a cell is running', which is a real design question with no good late answer. Secondary: F1 depends on web_bridge's state_before/state_after debug-panel plumbing, whose quality is unaudited, so it imports an unknown; and 'time travel' implies replay/undo semantics not yet designed. F2 rejected as first: it needs LiquidHandler backend hooks now living under pylabrobot.legacy.* (debt-1291), coupling this area to the wheel bump we declared out of scope. F4 rejected as first: no stress test means no evidence the API is right. Sequence: F3 proves the API, F1 second delivers the user-visible win.
- [ACCEPT] N1 — Vendor UPSTREAM master's visualizer JS rather than the pinned submodule's, decoupled from the PLR wheel bump: Spike-proven safe and strictly better. Upstream JS adds gridLayer, per-node visibility toggles, and method_registry which cuts the initial snapshot 461KB -> ~118KB (74.4%). Backward compatibility with an un-bumped 0.1.6 kernel is guaranteed by lib.js:967-968's `resourceData.methods || methodRegistry[resourceData.type] || []` precedence — per-resource methods win, which is exactly why the 0.1.6 payload rendered on the upstream renderer with 0 console errors. STANDARD_FORM_JSON_VERSION is 0.1.0 on both sides. Therefore the JS bump and the wheel bump (debt-1289/1291) are INDEPENDENTLY SEQUENCEABLE and must not be coupled in the plan — this deliberately de-risks the critical-path wheel blocker by removing it from this workstream's dependencies. The 4 vis.js patch anchors are byte-identical between pinned and upstream, so one D2 patch script serves both and the choice is reversible.
- [ACCEPT] CDN vendoring policy: Konva vendored+hash-pinned (mandatory), jszip deleted, html2canvas+gif.js lazy-loaded, bootstrap restyled out: Directly spike-determined, not inferred. All five CDN assets were removed and only konva.min.js vendored locally: the deck rendered perfectly with 0 console errors and 0 failed requests on BOTH the pinned and upstream renderers. jszip has ZERO references in lib.js (dead code — delete, do not vendor). html2canvas has exactly 3 references, all inside the GIF-recording path at lib.js:2838, never on the render path — so it must be lazy, since a static offline lab page must not block on a third-party host for a feature nobody used yet. Bootstrap is cosmetic only (.btn on 3 GIF-panel buttons, .navbar/.container-fluid); PLR's own main.css already supplies .toolbar-btn/.navbar-divider/.tree-action-btn, so restyle those 3 buttons rather than vendoring a CSS framework. Total offline bundle 860KB, mandatory render path ~420KB — a non-issue for a static page. Ownership: the V1 vendor script in CI, never a human.
- [DEFER] T4 — Jupyter-native transport (comm protocol / _repr_mimebundle_, visualizer as a notebook output cell): Genuinely attractive and not disproven — the standalone spike incidentally showed widgetsnbextension IS present in the built JupyterLite extension index, so the comm machinery exists in the target runtime. Deferred for three reasons. (1) It conflates transport with placement: it silently decides the visualizer lives in an output cell rather than a side pane, which is a PRODUCT decision about the notebook UX that this session was not scoped to make. (2) Payload cost lands in the wrong place — a set_root_resource snapshot is 461KB now / ~118KB after method_registry, and routing that through the notebook's own message channel means it can end up serialized INTO the saved .ipynb, which directly degrades the 'savable notebooks' product requirement. (3) It buys drift resilience against a surface (our own postMessage bridge) that we fully control and that is ~40 lines — the wrong thing to spend an abstraction on. Revisit if multi-visualizer-per-notebook or notebook-embedded static rendering becomes a requirement; the Tier-1 middleware boundary is deliberately transport-agnostic so this remains swappable.
- [ACCEPT] INVEST gate finding: winner fails 'S' (Small) as a single unit — ~20 acceptance criteria across 4 workstreams: Recorded as a real gap, not waived. Resolution is decomposition, not override: the artifact is the ARCHITECTURE for the design area, and it is cut into four independently shippable INVEST-sized slices plus one precondition. S0 (PRECONDITION, not a slice) — join spike: drive the vendored renderer from a live Pyodide-hosted Visualizer with an injected duck-socket, to determine whether the 3-method subclass suffices or T3 (own the pump) is required; the estimate is bimodal until this runs and nothing downstream should be committed before it. S1 — vendor+patch pipeline (V1 script, D2 anchors, konva vendored, jszip deleted, html2canvas/gif.js lazy, bootstrap restyled out); ships an offline-capable visualizer driven by fixtures; fully spike-proven, independently valuable, ~5 criteria. S2 — Python transport (PraxisBrowserVisualizer + duck sink + D4 sentinels); ships a live deck from the kernel; ~5 criteria. S3 — augmentation API (Tier 1 middleware, Tier 2 lib-adapter, MECHANICAL boundary enforcement per pre-mortem Failure 1, D1 golden render); ~6 criteria. S4 — first augmentation F3 click-to-Python, constrained to emit visible re-runnable code into a cell and never hidden state, per pre-mortem Failure 3; ~4 criteria. Each slice independently satisfies I/N/V/E/S/T; the composite satisfies all but S, and S is discharged by the cut.

## Assumptions

## TBDs

## Pre-mortem Record
**User:** PRE-MORTEM — six months on, it has definitively failed. Most likely stories, ordered by probability:

FAILURE 1 (most likely) — The tier boundary was never enforced, so it stopped existing. We shipped Tier 1 and Tier 2 as a documented distinction, but the lib-adapter is just a JS module and nothing PREVENTS an augmentation from writing `stage` or `resources` directly. The first time someone needed a geometry read the adapter did not expose, they reached past it, because that is a 1-line change and extending the adapter is a 20-line change plus a contract test. Six months later, eleven augmentations each touch lib.js internals directly, the adapter is vestigial, and a PLR bump breaks all eleven. The stability promise we made to authors was false, which is worse than never making it. PREVENTION: the boundary must be mechanically enforced, not documented — augmentations load in a scope where the lib.js globals are not reachable, plus a lint rule that fails CI on any reference to `stage`/`resources`/`.group` outside lib-adapter.js. If we cannot enforce it, we should not claim the two tiers exist.

FAILURE 2 — D4 became a rubber stamp. The source-hash sentinel went red on a whitespace change in month two. Someone bumped the hash without reading the diff, because that is what the failure mode of a hash sentinel is. In month five upstream changed the data dict for resource_assigned; the hash went red; someone bumped it again; resources silently stopped appearing when assigned after initial load. This is the runner-up's sharpest criticism and it is correct as stated. PREVENTION: D4 must not be a bare hash. It must print the actual diff against the recorded source and require the bumper to also re-record a captured ENVELOPE fixture from the new upstream — so bumping is not free, and the thing you re-record is the thing that actually matters (the wire payload), not the source text.

FAILURE 3 — The upward path (F3/A5) was under-designed at the kernel-busy boundary and poisoned the REPL. A click arrives while a cell is running. We queue it. The cell raises. The queue is now inconsistent with kernel state, or the click executes against a stale resource tree, or worst, we injected code into the REPL that the user did not type and cannot reproduce by re-running the notebook. The 'savable notebook' promise breaks: the notebook no longer reproduces its own outputs, because some of its state came from clicks. PREVENTION: F3 must never inject hidden state. It should write a real, visible, re-runnable line of code into the next cell — the click is a code-authoring convenience, not a side channel. Anything else and the notebook stops being a reproducible artifact, which is the whole product.

FAILURE 4 — D1 was disabled. The golden-render test needs unsandboxed headless Chromium and a pre-cached browser binary; the spike proved neither is available under the default sandbox and that Playwright's expected revision was not downloadable. Someone marked it allow-failure during a red CI day, and it stayed that way. Then lib.js drift landed silently. PREVENTION: if D1 cannot be made reliable in CI on day one, it is not a detector, and we must NOT count it as one of three — we would be shipping the winner's cost with two thirds of its safety.

FAILURE 5 (the one that kills the whole area, not just the design) — the assumption I flagged in the forcing beat turned out false in a way we did not catch. If pyodide's runtime cannot drive upstream's send_command coroutine to completion synchronously, or if a future upstream change makes those callback paths genuinely await, the whole 'reuse upstream's pump' premise collapses and we are doing T3 anyway, six months late. PREVENTION: spike it BEFORE the spec is trusted. Nothing in either executed spike drove the renderer from a live Pyodide-hosted Visualizer — each half was proven separately and the join was never tested.

None of these five change the winner. Three of them (1, 2, 4) change what the spec must REQUIRE, and I want those folded in as hard acceptance criteria rather than nice-to-haves. Failure 5 changes the sequencing: a join spike is a precondition, not a task.
**AI:** _not recorded_

## Acceptance Criteria
**Given** FIXED (cannot change):
- F1. The renderer stays a vendored third-party artifact. lib.js is 5867 lines of Konva code we do not own, do not understand in detail, and will re-vendor from upstream repeatedly. Zero edits to it. Non-negotiable.
- F2. No websocket server, no localhost:2121/:1337. Everything runs in the browser. The Python side is a Pyodide kernel in a Web Worker; the renderer is DOM-side. They are separated by a worker boundary (structured clone), not a socket.
- F3. The page must be servable as static files. No server-side rendering, no build-time-only-in-Angular tricks. GH Pages is the deploy target.
- F4. PLR's asyncio callback plumbing (visualizer.py:140-151, :628-682) cannot be used as-is — self.loop.call_soon_threadsafe blows up without a running loop (proven: spot.tracker.commit() -> 'Event loop has not been started.').
- F5. The wire format (STANDARD_FORM_JSON_VERSION 0.1.0, the {event, id, data} envelope, set_root_resource/resource_assigned/resource_unassigned/set_state) is upstream's, not ours. We consume it; we do not get to redesign it.

NEGOTIABLE:
- N1. Whether we vendor the PINNED submodule's visualizer JS or UPSTREAM master's. Spike says upstream is strictly better (gridLayer, visibility toggles, method_registry cutting 461KB -> 118KB) and is backward-compatible with an un-bumped 0.1.6 kernel via lib.js:967-968's `resourceData.methods || methodRegistry[resourceData.type]` precedence.
- N2. Whether the Python side uses PLR's Visualizer class at all, or bypasses it and calls the pure serializers directly (_serialize_with_methods, _sanitize_floats, _assemble_command — proven callable with no server).
- N3. Whether vis.js is patched, replaced wholesale, or shimmed from outside without touching the file.
- N4. Where the visualizer lives relative to the notebook: separate iframe, sibling pane, or embedded output cell.
- N5. Whether augmentations are Python-authored, JS-authored, or both.
- N6. How much of upstream's GIF/export UI we keep.

SHARPEST TENSION I want the divergence to attack: criterion A (drift resilience) and criterion D (augmentations survive lib.js INTERNAL refactors) pull in opposite directions from criterion E (small enough to ship). The cheapest augmentation seam is the one the spike proved — reach into lib.js's `resources[name].group` and `stage` globals — but that is precisely a dependency on lib.js INTERNALS, which is the thing criterion D says must not break us. I want at least one candidate that refuses that shortcut and pays a different price.

HIDDEN ASSUMPTION I want surfaced: everyone assumes the visualizer is a passive display. The user asked for "augmentations", which may imply INPUT — clicking a well to get a Python handle, dragging a plate to reposition it. If augmentations are bidirectional, the transport seam is not one-way and several otherwise-attractive designs die.
**When** implementing TWO-TIER WRAP. Python transport seam = duck-typed sink injected into an otherwise-untouched upstream Visualizer, plus a 3-method subclass replacing only the asyncio-welded callbacks (PraxisBrowserVisualizer). JS transport seam = upstream vis.js vendored and patched at 4 asserted anchors, replacing the websocket with a postMessage receiver. Augmentation API = two explicitly versioned tiers: Tier 1 middleware over the wire envelope (stable, bidirectional, drawing-incapable), Tier 2 canvas access through a single narrow lib-adapter (privileged, may break on PLR bumps). Vendoring = hash-pinned V1 script in CI vendoring upstream JS + konva.min.js, jszip deleted, html2canvas/gif.js lazy, bootstrap restyled out. Drift detection = three complementary detectors: D2 anchor assertions at patch time, D4 source-hash sentinels on the shadowed Python methods at unit-test time, D1 golden-render contract test at CI render time. First augmentation = F3 click-to-Python, chosen to retire the unproven upward-path risk; F1 state-diff highlight second.
**Then**
  - [ ] _add specific measurable criteria_
